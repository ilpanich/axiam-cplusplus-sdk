// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.


#include <stdexcept>

#include "management_json.hpp"

namespace axiam::management {

std::string to_wire(ActorType value) {
    switch (value) {
        case ActorType::User: return "User";
        case ActorType::ServiceAccount: return "ServiceAccount";
        case ActorType::System: return "System";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "User";
}

ActorType actor_type_from_wire(const std::string& value) {
    if (value == "User") return ActorType::User;
    if (value == "ServiceAccount") return ActorType::ServiceAccount;
    if (value == "System") return ActorType::System;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown ActorType value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const ActorType& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, ActorType& value) {
    value = actor_type_from_wire(j.get<std::string>());
}

std::string to_wire(AttestationMode value) {
    switch (value) {
        case AttestationMode::None: return "none";
        case AttestationMode::Indirect: return "indirect";
        case AttestationMode::DirectRequired: return "direct_required";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "none";
}

AttestationMode attestation_mode_from_wire(const std::string& value) {
    if (value == "none") return AttestationMode::None;
    if (value == "indirect") return AttestationMode::Indirect;
    if (value == "direct_required") return AttestationMode::DirectRequired;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown AttestationMode value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const AttestationMode& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, AttestationMode& value) {
    value = attestation_mode_from_wire(j.get<std::string>());
}

std::string to_wire(AuditOutcome value) {
    switch (value) {
        case AuditOutcome::Success: return "Success";
        case AuditOutcome::Failure: return "Failure";
        case AuditOutcome::Denied: return "Denied";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Success";
}

AuditOutcome audit_outcome_from_wire(const std::string& value) {
    if (value == "Success") return AuditOutcome::Success;
    if (value == "Failure") return AuditOutcome::Failure;
    if (value == "Denied") return AuditOutcome::Denied;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown AuditOutcome value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const AuditOutcome& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, AuditOutcome& value) {
    value = audit_outcome_from_wire(j.get<std::string>());
}

std::string to_wire(CertificateStatus value) {
    switch (value) {
        case CertificateStatus::Active: return "Active";
        case CertificateStatus::Revoked: return "Revoked";
        case CertificateStatus::Expired: return "Expired";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Active";
}

CertificateStatus certificate_status_from_wire(const std::string& value) {
    if (value == "Active") return CertificateStatus::Active;
    if (value == "Revoked") return CertificateStatus::Revoked;
    if (value == "Expired") return CertificateStatus::Expired;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown CertificateStatus value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const CertificateStatus& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, CertificateStatus& value) {
    value = certificate_status_from_wire(j.get<std::string>());
}

std::string to_wire(CertificateType value) {
    switch (value) {
        case CertificateType::User: return "User";
        case CertificateType::Service: return "Service";
        case CertificateType::Device: return "Device";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "User";
}

CertificateType certificate_type_from_wire(const std::string& value) {
    if (value == "User") return CertificateType::User;
    if (value == "Service") return CertificateType::Service;
    if (value == "Device") return CertificateType::Device;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown CertificateType value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const CertificateType& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, CertificateType& value) {
    value = certificate_type_from_wire(j.get<std::string>());
}

std::string to_wire(CertificationLevel value) {
    switch (value) {
        case CertificationLevel::L1: return "L1";
        case CertificationLevel::L1Plus: return "L1Plus";
        case CertificationLevel::L2: return "L2";
        case CertificationLevel::L2Plus: return "L2Plus";
        case CertificationLevel::L3: return "L3";
        case CertificationLevel::L3Plus: return "L3Plus";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "L1";
}

CertificationLevel certification_level_from_wire(const std::string& value) {
    if (value == "L1") return CertificationLevel::L1;
    if (value == "L1Plus") return CertificationLevel::L1Plus;
    if (value == "L2") return CertificationLevel::L2;
    if (value == "L2Plus") return CertificationLevel::L2Plus;
    if (value == "L3") return CertificationLevel::L3;
    if (value == "L3Plus") return CertificationLevel::L3Plus;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown CertificationLevel value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const CertificationLevel& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, CertificationLevel& value) {
    value = certification_level_from_wire(j.get<std::string>());
}

std::string to_wire(ClientAuthMethod value) {
    switch (value) {
        case ClientAuthMethod::ClientSecretPost: return "client_secret_post";
        case ClientAuthMethod::TlsClientAuth: return "tls_client_auth";
        case ClientAuthMethod::SelfSignedTlsClientAuth: return "self_signed_tls_client_auth";
        case ClientAuthMethod::PrivateKeyJwt: return "private_key_jwt";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "client_secret_post";
}

ClientAuthMethod client_auth_method_from_wire(const std::string& value) {
    if (value == "client_secret_post") return ClientAuthMethod::ClientSecretPost;
    if (value == "tls_client_auth") return ClientAuthMethod::TlsClientAuth;
    if (value == "self_signed_tls_client_auth") return ClientAuthMethod::SelfSignedTlsClientAuth;
    if (value == "private_key_jwt") return ClientAuthMethod::PrivateKeyJwt;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown ClientAuthMethod value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const ClientAuthMethod& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, ClientAuthMethod& value) {
    value = client_auth_method_from_wire(j.get<std::string>());
}

std::string to_wire(ClientProfile value) {
    switch (value) {
        case ClientProfile::Standard: return "standard";
        case ClientProfile::Fapi2: return "fapi2";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "standard";
}

ClientProfile client_profile_from_wire(const std::string& value) {
    if (value == "standard") return ClientProfile::Standard;
    if (value == "fapi2") return ClientProfile::Fapi2;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown ClientProfile value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const ClientProfile& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, ClientProfile& value) {
    value = client_profile_from_wire(j.get<std::string>());
}

std::string to_wire(FailurePolicy value) {
    switch (value) {
        case FailurePolicy::FailClosed: return "fail_closed";
        case FailurePolicy::FailOpen: return "fail_open";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "fail_closed";
}

FailurePolicy failure_policy_from_wire(const std::string& value) {
    if (value == "fail_closed") return FailurePolicy::FailClosed;
    if (value == "fail_open") return FailurePolicy::FailOpen;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown FailurePolicy value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const FailurePolicy& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, FailurePolicy& value) {
    value = failure_policy_from_wire(j.get<std::string>());
}

std::string to_wire(KeyAlgorithm value) {
    switch (value) {
        case KeyAlgorithm::Rsa4096: return "Rsa4096";
        case KeyAlgorithm::Ed25519: return "Ed25519";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Rsa4096";
}

KeyAlgorithm key_algorithm_from_wire(const std::string& value) {
    if (value == "Rsa4096") return KeyAlgorithm::Rsa4096;
    if (value == "Ed25519") return KeyAlgorithm::Ed25519;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown KeyAlgorithm value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const KeyAlgorithm& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, KeyAlgorithm& value) {
    value = key_algorithm_from_wire(j.get<std::string>());
}

std::string to_wire(MfaMethodType value) {
    switch (value) {
        case MfaMethodType::Totp: return "Totp";
        case MfaMethodType::Passkey: return "Passkey";
        case MfaMethodType::SecurityKey: return "SecurityKey";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Totp";
}

MfaMethodType mfa_method_type_from_wire(const std::string& value) {
    if (value == "Totp") return MfaMethodType::Totp;
    if (value == "Passkey") return MfaMethodType::Passkey;
    if (value == "SecurityKey") return MfaMethodType::SecurityKey;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown MfaMethodType value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const MfaMethodType& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, MfaMethodType& value) {
    value = mfa_method_type_from_wire(j.get<std::string>());
}

std::string to_wire(NotificationEventType value) {
    switch (value) {
        case NotificationEventType::LoginFailure: return "login_failure";
        case NotificationEventType::AccountLocked: return "account_locked";
        case NotificationEventType::MfaEnrollmentChanged: return "mfa_enrollment_changed";
        case NotificationEventType::PasswordChanged: return "password_changed";
        case NotificationEventType::PasswordResetRequested: return "password_reset_requested";
        case NotificationEventType::RoleAssigned: return "role_assigned";
        case NotificationEventType::RoleUnassigned: return "role_unassigned";
        case NotificationEventType::PermissionGranted: return "permission_granted";
        case NotificationEventType::PermissionRevoked: return "permission_revoked";
        case NotificationEventType::CertificateIssued: return "certificate_issued";
        case NotificationEventType::CertificateRevoked: return "certificate_revoked";
        case NotificationEventType::CaCertificateRevoked: return "ca_certificate_revoked";
        case NotificationEventType::UserCreated: return "user_created";
        case NotificationEventType::UserDeleted: return "user_deleted";
        case NotificationEventType::UserUpdated: return "user_updated";
        case NotificationEventType::ServiceAccountCreated: return "service_account_created";
        case NotificationEventType::ServiceAccountDeleted: return "service_account_deleted";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "login_failure";
}

NotificationEventType notification_event_type_from_wire(const std::string& value) {
    if (value == "login_failure") return NotificationEventType::LoginFailure;
    if (value == "account_locked") return NotificationEventType::AccountLocked;
    if (value == "mfa_enrollment_changed") return NotificationEventType::MfaEnrollmentChanged;
    if (value == "password_changed") return NotificationEventType::PasswordChanged;
    if (value == "password_reset_requested") return NotificationEventType::PasswordResetRequested;
    if (value == "role_assigned") return NotificationEventType::RoleAssigned;
    if (value == "role_unassigned") return NotificationEventType::RoleUnassigned;
    if (value == "permission_granted") return NotificationEventType::PermissionGranted;
    if (value == "permission_revoked") return NotificationEventType::PermissionRevoked;
    if (value == "certificate_issued") return NotificationEventType::CertificateIssued;
    if (value == "certificate_revoked") return NotificationEventType::CertificateRevoked;
    if (value == "ca_certificate_revoked") return NotificationEventType::CaCertificateRevoked;
    if (value == "user_created") return NotificationEventType::UserCreated;
    if (value == "user_deleted") return NotificationEventType::UserDeleted;
    if (value == "user_updated") return NotificationEventType::UserUpdated;
    if (value == "service_account_created") return NotificationEventType::ServiceAccountCreated;
    if (value == "service_account_deleted") return NotificationEventType::ServiceAccountDeleted;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown NotificationEventType value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const NotificationEventType& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, NotificationEventType& value) {
    value = notification_event_type_from_wire(j.get<std::string>());
}

std::string to_wire(PermissionEffect value) {
    switch (value) {
        case PermissionEffect::Allow: return "allow";
        case PermissionEffect::Deny: return "deny";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "allow";
}

PermissionEffect permission_effect_from_wire(const std::string& value) {
    if (value == "allow") return PermissionEffect::Allow;
    if (value == "deny") return PermissionEffect::Deny;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown PermissionEffect value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const PermissionEffect& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, PermissionEffect& value) {
    value = permission_effect_from_wire(j.get<std::string>());
}

std::string to_wire(PgpKeyAlgorithm value) {
    switch (value) {
        case PgpKeyAlgorithm::Rsa4096: return "Rsa4096";
        case PgpKeyAlgorithm::Ed25519: return "Ed25519";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Rsa4096";
}

PgpKeyAlgorithm pgp_key_algorithm_from_wire(const std::string& value) {
    if (value == "Rsa4096") return PgpKeyAlgorithm::Rsa4096;
    if (value == "Ed25519") return PgpKeyAlgorithm::Ed25519;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown PgpKeyAlgorithm value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const PgpKeyAlgorithm& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, PgpKeyAlgorithm& value) {
    value = pgp_key_algorithm_from_wire(j.get<std::string>());
}

std::string to_wire(PgpKeyPurpose value) {
    switch (value) {
        case PgpKeyPurpose::AuditSigning: return "AuditSigning";
        case PgpKeyPurpose::Export_: return "Export";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "AuditSigning";
}

PgpKeyPurpose pgp_key_purpose_from_wire(const std::string& value) {
    if (value == "AuditSigning") return PgpKeyPurpose::AuditSigning;
    if (value == "Export") return PgpKeyPurpose::Export_;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown PgpKeyPurpose value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const PgpKeyPurpose& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, PgpKeyPurpose& value) {
    value = pgp_key_purpose_from_wire(j.get<std::string>());
}

std::string to_wire(PgpKeyStatus value) {
    switch (value) {
        case PgpKeyStatus::Active: return "Active";
        case PgpKeyStatus::Revoked: return "Revoked";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Active";
}

PgpKeyStatus pgp_key_status_from_wire(const std::string& value) {
    if (value == "Active") return PgpKeyStatus::Active;
    if (value == "Revoked") return PgpKeyStatus::Revoked;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown PgpKeyStatus value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const PgpKeyStatus& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, PgpKeyStatus& value) {
    value = pgp_key_status_from_wire(j.get<std::string>());
}

std::string to_wire(ReactorMode value) {
    switch (value) {
        case ReactorMode::Intercept: return "intercept";
        case ReactorMode::Listen: return "listen";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "intercept";
}

ReactorMode reactor_mode_from_wire(const std::string& value) {
    if (value == "intercept") return ReactorMode::Intercept;
    if (value == "listen") return ReactorMode::Listen;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown ReactorMode value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const ReactorMode& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, ReactorMode& value) {
    value = reactor_mode_from_wire(j.get<std::string>());
}

std::string to_wire(ScimTokenStatus value) {
    switch (value) {
        case ScimTokenStatus::Active: return "active";
        case ScimTokenStatus::Expired: return "expired";
        case ScimTokenStatus::Revoked: return "revoked";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "active";
}

ScimTokenStatus scim_token_status_from_wire(const std::string& value) {
    if (value == "active") return ScimTokenStatus::Active;
    if (value == "expired") return ScimTokenStatus::Expired;
    if (value == "revoked") return ScimTokenStatus::Revoked;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown ScimTokenStatus value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const ScimTokenStatus& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, ScimTokenStatus& value) {
    value = scim_token_status_from_wire(j.get<std::string>());
}

std::string to_wire(SettingsScope value) {
    switch (value) {
        case SettingsScope::Org: return "Org";
        case SettingsScope::Tenant: return "Tenant";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Org";
}

SettingsScope settings_scope_from_wire(const std::string& value) {
    if (value == "Org") return SettingsScope::Org;
    if (value == "Tenant") return SettingsScope::Tenant;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown SettingsScope value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const SettingsScope& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, SettingsScope& value) {
    value = settings_scope_from_wire(j.get<std::string>());
}

std::string to_wire(TenantStatus value) {
    switch (value) {
        case TenantStatus::Active: return "Active";
        case TenantStatus::Suspended: return "Suspended";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Active";
}

TenantStatus tenant_status_from_wire(const std::string& value) {
    if (value == "Active") return TenantStatus::Active;
    if (value == "Suspended") return TenantStatus::Suspended;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown TenantStatus value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const TenantStatus& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, TenantStatus& value) {
    value = tenant_status_from_wire(j.get<std::string>());
}

std::string to_wire(UnknownAaguidAction value) {
    switch (value) {
        case UnknownAaguidAction::Allow: return "allow";
        case UnknownAaguidAction::Deny: return "deny";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "allow";
}

UnknownAaguidAction unknown_aaguid_action_from_wire(const std::string& value) {
    if (value == "allow") return UnknownAaguidAction::Allow;
    if (value == "deny") return UnknownAaguidAction::Deny;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown UnknownAaguidAction value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const UnknownAaguidAction& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, UnknownAaguidAction& value) {
    value = unknown_aaguid_action_from_wire(j.get<std::string>());
}

std::string to_wire(UserStatus value) {
    switch (value) {
        case UserStatus::Active: return "Active";
        case UserStatus::Inactive: return "Inactive";
        case UserStatus::Locked: return "Locked";
        case UserStatus::PendingVerification: return "PendingVerification";
        case UserStatus::Anonymized: return "Anonymized";
        case UserStatus::Deleted: return "Deleted";
    }
    // Unreachable for a value produced by this SDK; present because a switch over an enum class
    // with an out-of-range value is otherwise undefined.
    return "Active";
}

UserStatus user_status_from_wire(const std::string& value) {
    if (value == "Active") return UserStatus::Active;
    if (value == "Inactive") return UserStatus::Inactive;
    if (value == "Locked") return UserStatus::Locked;
    if (value == "PendingVerification") return UserStatus::PendingVerification;
    if (value == "Anonymized") return UserStatus::Anonymized;
    if (value == "Deleted") return UserStatus::Deleted;
    // No default enumerator, on purpose: an unrecognised value is reported rather than mapped
    // to whichever one happens to be first.
    throw std::invalid_argument("unknown UserStatus value \"" + value + "\" -- the server may be newer than this SDK");
}

void to_json(nlohmann::json& j, const UserStatus& value) { j = to_wire(value); }
void from_json(const nlohmann::json& j, UserStatus& value) {
    value = user_status_from_wire(j.get<std::string>());
}

void to_json(nlohmann::json& j, const AddMemberRequest& value) {
    j = nlohmann::json::object();
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, AddMemberRequest& value) {
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const ApiProviderConfig& value) {
    j = nlohmann::json::object();
    if (value.api_url) {
        j["api_url"] = *value.api_url;
    }
}

void from_json(const nlohmann::json& j, ApiProviderConfig& value) {
    if (auto it = j.find("api_url"); it != j.end() && !it->is_null()) {
        value.api_url = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const AssignRoleToGroupRequest& value) {
    j = nlohmann::json::object();
    j["group_id"] = value.group_id;
    if (value.resource_id) {
        j["resource_id"] = *value.resource_id;
    }
}

void from_json(const nlohmann::json& j, AssignRoleToGroupRequest& value) {
    value.group_id = j.at("group_id").get<std::string>();
    if (auto it = j.find("resource_id"); it != j.end() && !it->is_null()) {
        value.resource_id = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const AssignRoleToUserRequest& value) {
    j = nlohmann::json::object();
    if (value.resource_id) {
        j["resource_id"] = *value.resource_id;
    }
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, AssignRoleToUserRequest& value) {
    if (auto it = j.find("resource_id"); it != j.end() && !it->is_null()) {
        value.resource_id = it->get<std::string>();
    }
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const AuditLogEntry& value) {
    j = nlohmann::json::object();
    j["action"] = value.action;
    j["actor_id"] = value.actor_id;
    j["actor_type"] = to_wire(value.actor_type);
    j["id"] = value.id;
    if (value.ip_address) {
        j["ip_address"] = *value.ip_address;
    }
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["outcome"] = to_wire(value.outcome);
    if (value.resource_id) {
        j["resource_id"] = *value.resource_id;
    }
    j["tenant_id"] = value.tenant_id;
    j["timestamp"] = value.timestamp;
}

void from_json(const nlohmann::json& j, AuditLogEntry& value) {
    value.action = j.at("action").get<std::string>();
    value.actor_id = j.at("actor_id").get<std::string>();
    value.actor_type = actor_type_from_wire(j.at("actor_type").get<std::string>());
    value.id = j.at("id").get<std::string>();
    if (auto it = j.find("ip_address"); it != j.end() && !it->is_null()) {
        value.ip_address = it->get<std::string>();
    }
    value.metadata = j.at("metadata").dump();
    value.outcome = audit_outcome_from_wire(j.at("outcome").get<std::string>());
    if (auto it = j.find("resource_id"); it != j.end() && !it->is_null()) {
        value.resource_id = it->get<std::string>();
    }
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.timestamp = j.at("timestamp").get<std::string>();
}

void to_json(nlohmann::json& j, const BindCertificate& value) {
    j = nlohmann::json::object();
    j["certificate_id"] = value.certificate_id;
}

void from_json(const nlohmann::json& j, BindCertificate& value) {
    value.certificate_id = j.at("certificate_id").get<std::string>();
}

void to_json(nlohmann::json& j, const CaCertificate& value) {
    j = nlohmann::json::object();
    if (value.chain_pem) {
        j["chain_pem"] = *value.chain_pem;
    }
    j["created_at"] = value.created_at;
    j["fingerprint"] = value.fingerprint;
    j["id"] = value.id;
    j["key_algorithm"] = to_wire(value.key_algorithm);
    if (value.key_custody) {
        j["key_custody"] = *value.key_custody;
    }
    if (value.key_locator) {
        j["key_locator"] = *value.key_locator;
    }
    if (value.mtls_trust_anchor) {
        j["mtls_trust_anchor"] = *value.mtls_trust_anchor;
    }
    j["not_after"] = value.not_after;
    j["not_before"] = value.not_before;
    j["organization_id"] = value.organization_id;
    if (value.parent_ca_id) {
        j["parent_ca_id"] = *value.parent_ca_id;
    }
    j["public_cert_pem"] = value.public_cert_pem;
    j["status"] = to_wire(value.status);
    j["subject"] = value.subject;
    if (value.tenant_id) {
        j["tenant_id"] = *value.tenant_id;
    }
}

void from_json(const nlohmann::json& j, CaCertificate& value) {
    if (auto it = j.find("chain_pem"); it != j.end() && !it->is_null()) {
        value.chain_pem = it->get<std::string>();
    }
    value.created_at = j.at("created_at").get<std::string>();
    value.fingerprint = j.at("fingerprint").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    if (auto it = j.find("key_custody"); it != j.end() && !it->is_null()) {
        value.key_custody = it->get<std::string>();
    }
    if (auto it = j.find("key_locator"); it != j.end() && !it->is_null()) {
        value.key_locator = it->get<std::string>();
    }
    if (auto it = j.find("mtls_trust_anchor"); it != j.end() && !it->is_null()) {
        value.mtls_trust_anchor = it->get<bool>();
    }
    value.not_after = j.at("not_after").get<std::string>();
    value.not_before = j.at("not_before").get<std::string>();
    value.organization_id = j.at("organization_id").get<std::string>();
    if (auto it = j.find("parent_ca_id"); it != j.end() && !it->is_null()) {
        value.parent_ca_id = it->get<std::string>();
    }
    value.public_cert_pem = j.at("public_cert_pem").get<std::string>();
    value.status = certificate_status_from_wire(j.at("status").get<std::string>());
    value.subject = j.at("subject").get<std::string>();
    if (auto it = j.find("tenant_id"); it != j.end() && !it->is_null()) {
        value.tenant_id = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const Certificate& value) {
    j = nlohmann::json::object();
    j["cert_type"] = to_wire(value.cert_type);
    j["created_at"] = value.created_at;
    j["fingerprint"] = value.fingerprint;
    j["id"] = value.id;
    j["issuer_ca_id"] = value.issuer_ca_id;
    j["key_algorithm"] = to_wire(value.key_algorithm);
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["not_after"] = value.not_after;
    j["not_before"] = value.not_before;
    j["public_cert_pem"] = value.public_cert_pem;
    j["status"] = to_wire(value.status);
    j["subject"] = value.subject;
    j["tenant_id"] = value.tenant_id;
}

void from_json(const nlohmann::json& j, Certificate& value) {
    value.cert_type = certificate_type_from_wire(j.at("cert_type").get<std::string>());
    value.created_at = j.at("created_at").get<std::string>();
    value.fingerprint = j.at("fingerprint").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.issuer_ca_id = j.at("issuer_ca_id").get<std::string>();
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    value.metadata = j.at("metadata").dump();
    value.not_after = j.at("not_after").get<std::string>();
    value.not_before = j.at("not_before").get<std::string>();
    value.public_cert_pem = j.at("public_cert_pem").get<std::string>();
    value.status = certificate_status_from_wire(j.at("status").get<std::string>());
    value.subject = j.at("subject").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
}

void to_json(nlohmann::json& j, const CertificatePolicy& value) {
    j = nlohmann::json::object();
    j["default_cert_validity_days"] = value.default_cert_validity_days;
    j["max_cert_validity_days"] = value.max_cert_validity_days;
}

void from_json(const nlohmann::json& j, CertificatePolicy& value) {
    value.default_cert_validity_days = j.at("default_cert_validity_days").get<std::int64_t>();
    value.max_cert_validity_days = j.at("max_cert_validity_days").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const ComplianceReportEntry& value) {
    j = nlohmann::json::object();
    if (value.aaguid) {
        j["aaguid"] = *value.aaguid;
    }
    if (value.authenticator_name) {
        j["authenticator_name"] = *value.authenticator_name;
    }
    j["compliant"] = value.compliant;
    j["credential_id"] = value.credential_id;
    j["name"] = value.name;
    if (value.reason) {
        j["reason"] = *value.reason;
    }
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, ComplianceReportEntry& value) {
    if (auto it = j.find("aaguid"); it != j.end() && !it->is_null()) {
        value.aaguid = it->get<std::string>();
    }
    if (auto it = j.find("authenticator_name"); it != j.end() && !it->is_null()) {
        value.authenticator_name = it->get<std::string>();
    }
    value.compliant = j.at("compliant").get<bool>();
    value.credential_id = j.at("credential_id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("reason"); it != j.end() && !it->is_null()) {
        value.reason = it->get<std::string>();
    }
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateCaCertificateRequest& value) {
    j = nlohmann::json::object();
    if (value.intermediate_subject) {
        j["intermediate_subject"] = *value.intermediate_subject;
    }
    if (value.intermediate_validity_days) {
        j["intermediate_validity_days"] = *value.intermediate_validity_days;
    }
    if (value.issue_from_root) {
        j["issue_from_root"] = *value.issue_from_root;
    }
    j["key_algorithm"] = to_wire(value.key_algorithm);
    j["subject"] = value.subject;
    j["validity_days"] = value.validity_days;
}

void from_json(const nlohmann::json& j, CreateCaCertificateRequest& value) {
    if (auto it = j.find("intermediate_subject"); it != j.end() && !it->is_null()) {
        value.intermediate_subject = it->get<std::string>();
    }
    if (auto it = j.find("intermediate_validity_days"); it != j.end() && !it->is_null()) {
        value.intermediate_validity_days = it->get<std::int64_t>();
    }
    if (auto it = j.find("issue_from_root"); it != j.end() && !it->is_null()) {
        value.issue_from_root = it->get<bool>();
    }
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    value.subject = j.at("subject").get<std::string>();
    value.validity_days = j.at("validity_days").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const CreateCertificateRequest& value) {
    j = nlohmann::json::object();
    j["cert_type"] = to_wire(value.cert_type);
    j["issuer_ca_id"] = value.issuer_ca_id;
    j["key_algorithm"] = to_wire(value.key_algorithm);
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    j["subject"] = value.subject;
    j["validity_days"] = value.validity_days;
}

void from_json(const nlohmann::json& j, CreateCertificateRequest& value) {
    value.cert_type = certificate_type_from_wire(j.at("cert_type").get<std::string>());
    value.issuer_ca_id = j.at("issuer_ca_id").get<std::string>();
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    value.subject = j.at("subject").get<std::string>();
    value.validity_days = j.at("validity_days").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const TokenExchangeTrustRequest& value) {
    j = nlohmann::json::object();
    if (value.accepted_audiences) {
        j["accepted_audiences"] = *value.accepted_audiences;
    }
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    if (value.max_lifetime_secs) {
        j["max_lifetime_secs"] = *value.max_lifetime_secs;
    }
    if (value.max_token_age_secs) {
        j["max_token_age_secs"] = *value.max_token_age_secs;
    }
    if (value.scope_map) {
        auto parsed = nlohmann::json::parse(*value.scope_map, nullptr, false);
        if (!parsed.is_discarded()) j["scope_map"] = parsed;
    }
    if (value.subject_mapping) {
        j["subject_mapping"] = *value.subject_mapping;
    }
}

void from_json(const nlohmann::json& j, TokenExchangeTrustRequest& value) {
    if (auto it = j.find("accepted_audiences"); it != j.end() && !it->is_null()) {
        value.accepted_audiences = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    if (auto it = j.find("max_lifetime_secs"); it != j.end() && !it->is_null()) {
        value.max_lifetime_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("max_token_age_secs"); it != j.end() && !it->is_null()) {
        value.max_token_age_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("scope_map"); it != j.end() && !it->is_null()) {
        value.scope_map = it->dump();
    }
    if (auto it = j.find("subject_mapping"); it != j.end() && !it->is_null()) {
        value.subject_mapping = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const CreateFederationConfigRequest& value) {
    j = nlohmann::json::object();
    if (value.allowed_algorithms) {
        j["allowed_algorithms"] = *value.allowed_algorithms;
    }
    if (value.attribute_map) {
        auto parsed = nlohmann::json::parse(*value.attribute_map, nullptr, false);
        if (!parsed.is_discarded()) j["attribute_map"] = parsed;
    }
    j["client_id"] = value.client_id;
    j["client_secret"] = detail::reveal(value.client_secret);
    if (value.idp_signing_cert_pem) {
        j["idp_signing_cert_pem"] = *value.idp_signing_cert_pem;
    }
    if (value.metadata_url) {
        j["metadata_url"] = *value.metadata_url;
    }
    j["protocol"] = value.protocol;
    j["provider"] = value.provider;
    if (value.token_exchange) {
        j["token_exchange"] = *value.token_exchange;
    }
}

void from_json(const nlohmann::json& j, CreateFederationConfigRequest& value) {
    if (auto it = j.find("allowed_algorithms"); it != j.end() && !it->is_null()) {
        value.allowed_algorithms = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("attribute_map"); it != j.end() && !it->is_null()) {
        value.attribute_map = it->dump();
    }
    value.client_id = j.at("client_id").get<std::string>();
    value.client_secret = Sensitive<std::string>(j.at("client_secret").get<std::string>());
    if (auto it = j.find("idp_signing_cert_pem"); it != j.end() && !it->is_null()) {
        value.idp_signing_cert_pem = it->get<std::string>();
    }
    if (auto it = j.find("metadata_url"); it != j.end() && !it->is_null()) {
        value.metadata_url = it->get<std::string>();
    }
    value.protocol = j.at("protocol").get<std::string>();
    value.provider = j.at("provider").get<std::string>();
    if (auto it = j.find("token_exchange"); it != j.end() && !it->is_null()) {
        value.token_exchange = it->get<TokenExchangeTrustRequest>();
    }
}

void to_json(nlohmann::json& j, const CreateGroupRequest& value) {
    j = nlohmann::json::object();
    j["description"] = value.description;
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    j["name"] = value.name;
}

void from_json(const nlohmann::json& j, CreateGroupRequest& value) {
    value.description = j.at("description").get<std::string>();
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    value.name = j.at("name").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateIntermediateCaRequest& value) {
    j = nlohmann::json::object();
    j["key_algorithm"] = to_wire(value.key_algorithm);
    j["parent_ca_id"] = value.parent_ca_id;
    j["subject"] = value.subject;
    j["validity_days"] = value.validity_days;
}

void from_json(const nlohmann::json& j, CreateIntermediateCaRequest& value) {
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    value.parent_ca_id = j.at("parent_ca_id").get<std::string>();
    value.subject = j.at("subject").get<std::string>();
    value.validity_days = j.at("validity_days").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const CreateNotificationRuleRequest& value) {
    j = nlohmann::json::object();
    j["description"] = value.description;
    j["events"] = value.events;
    j["name"] = value.name;
    j["recipient_emails"] = value.recipient_emails;
}

void from_json(const nlohmann::json& j, CreateNotificationRuleRequest& value) {
    value.description = j.at("description").get<std::string>();
    value.events = j.at("events").get<std::vector<NotificationEventType>>();
    value.name = j.at("name").get<std::string>();
    value.recipient_emails = j.at("recipient_emails").get<std::vector<std::string>>();
}

void to_json(nlohmann::json& j, const CreateOAuth2ClientRequest& value) {
    j = nlohmann::json::object();
    if (value.backchannel_logout_uri) {
        j["backchannel_logout_uri"] = *value.backchannel_logout_uri;
    }
    if (value.dpop_bound_access_tokens) {
        j["dpop_bound_access_tokens"] = *value.dpop_bound_access_tokens;
    }
    if (value.dpop_require_nonce) {
        j["dpop_require_nonce"] = *value.dpop_require_nonce;
    }
    j["grant_types"] = value.grant_types;
    if (value.jwks) {
        j["jwks"] = *value.jwks;
    }
    if (value.jwks_uri) {
        j["jwks_uri"] = *value.jwks_uri;
    }
    j["name"] = value.name;
    if (value.post_logout_redirect_uris) {
        j["post_logout_redirect_uris"] = *value.post_logout_redirect_uris;
    }
    if (value.profile) {
        j["profile"] = to_wire(*value.profile);
    }
    j["redirect_uris"] = value.redirect_uris;
    if (value.require_par) {
        j["require_par"] = *value.require_par;
    }
    j["scopes"] = value.scopes;
    if (value.self_signed_tls_client_auth_thumbprints) {
        j["self_signed_tls_client_auth_thumbprints"] = *value.self_signed_tls_client_auth_thumbprints;
    }
    if (value.tls_client_auth_san_dns) {
        j["tls_client_auth_san_dns"] = *value.tls_client_auth_san_dns;
    }
    if (value.tls_client_auth_san_uri) {
        j["tls_client_auth_san_uri"] = *value.tls_client_auth_san_uri;
    }
    if (value.tls_client_auth_subject_dn) {
        j["tls_client_auth_subject_dn"] = *value.tls_client_auth_subject_dn;
    }
    if (value.tls_client_certificate_bound_access_tokens) {
        j["tls_client_certificate_bound_access_tokens"] = *value.tls_client_certificate_bound_access_tokens;
    }
    if (value.token_endpoint_auth_method) {
        j["token_endpoint_auth_method"] = to_wire(*value.token_endpoint_auth_method);
    }
}

void from_json(const nlohmann::json& j, CreateOAuth2ClientRequest& value) {
    if (auto it = j.find("backchannel_logout_uri"); it != j.end() && !it->is_null()) {
        value.backchannel_logout_uri = it->get<std::string>();
    }
    if (auto it = j.find("dpop_bound_access_tokens"); it != j.end() && !it->is_null()) {
        value.dpop_bound_access_tokens = it->get<bool>();
    }
    if (auto it = j.find("dpop_require_nonce"); it != j.end() && !it->is_null()) {
        value.dpop_require_nonce = it->get<bool>();
    }
    value.grant_types = j.at("grant_types").get<std::vector<std::string>>();
    if (auto it = j.find("jwks"); it != j.end() && !it->is_null()) {
        value.jwks = it->get<std::string>();
    }
    if (auto it = j.find("jwks_uri"); it != j.end() && !it->is_null()) {
        value.jwks_uri = it->get<std::string>();
    }
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("post_logout_redirect_uris"); it != j.end() && !it->is_null()) {
        value.post_logout_redirect_uris = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("profile"); it != j.end() && !it->is_null()) {
        value.profile = client_profile_from_wire(it->get<std::string>());
    }
    value.redirect_uris = j.at("redirect_uris").get<std::vector<std::string>>();
    if (auto it = j.find("require_par"); it != j.end() && !it->is_null()) {
        value.require_par = it->get<bool>();
    }
    value.scopes = j.at("scopes").get<std::vector<std::string>>();
    if (auto it = j.find("self_signed_tls_client_auth_thumbprints"); it != j.end() && !it->is_null()) {
        value.self_signed_tls_client_auth_thumbprints = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("tls_client_auth_san_dns"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_san_dns = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_auth_san_uri"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_san_uri = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_auth_subject_dn"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_subject_dn = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_certificate_bound_access_tokens"); it != j.end() && !it->is_null()) {
        value.tls_client_certificate_bound_access_tokens = it->get<bool>();
    }
    if (auto it = j.find("token_endpoint_auth_method"); it != j.end() && !it->is_null()) {
        value.token_endpoint_auth_method = client_auth_method_from_wire(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const CreatePermissionRequest& value) {
    j = nlohmann::json::object();
    j["action"] = value.action;
    j["description"] = value.description;
}

void from_json(const nlohmann::json& j, CreatePermissionRequest& value) {
    value.action = j.at("action").get<std::string>();
    value.description = j.at("description").get<std::string>();
}

void to_json(nlohmann::json& j, const CreatePgpKeyRequest& value) {
    j = nlohmann::json::object();
    j["algorithm"] = to_wire(value.algorithm);
    j["email"] = value.email;
    j["name"] = value.name;
    j["purpose"] = to_wire(value.purpose);
}

void from_json(const nlohmann::json& j, CreatePgpKeyRequest& value) {
    value.algorithm = pgp_key_algorithm_from_wire(j.at("algorithm").get<std::string>());
    value.email = j.at("email").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.purpose = pgp_key_purpose_from_wire(j.at("purpose").get<std::string>());
}

void to_json(nlohmann::json& j, const CreateReactorRequest& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    j["events"] = value.events;
    if (value.failure_policy) {
        j["failure_policy"] = to_wire(*value.failure_policy);
    }
    j["mode"] = to_wire(value.mode);
    j["name"] = value.name;
    if (value.priority) {
        j["priority"] = *value.priority;
    }
    if (value.timeout_ms) {
        j["timeout_ms"] = *value.timeout_ms;
    }
}

void from_json(const nlohmann::json& j, CreateReactorRequest& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    value.events = j.at("events").get<std::vector<std::string>>();
    if (auto it = j.find("failure_policy"); it != j.end() && !it->is_null()) {
        value.failure_policy = failure_policy_from_wire(it->get<std::string>());
    }
    value.mode = reactor_mode_from_wire(j.at("mode").get<std::string>());
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("priority"); it != j.end() && !it->is_null()) {
        value.priority = it->get<std::int64_t>();
    }
    if (auto it = j.find("timeout_ms"); it != j.end() && !it->is_null()) {
        value.timeout_ms = it->get<std::int64_t>();
    }
}

void to_json(nlohmann::json& j, const CreateResourceRequest& value) {
    j = nlohmann::json::object();
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    j["name"] = value.name;
    if (value.parent_id) {
        j["parent_id"] = *value.parent_id;
    }
    j["resource_type"] = value.resource_type;
}

void from_json(const nlohmann::json& j, CreateResourceRequest& value) {
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("parent_id"); it != j.end() && !it->is_null()) {
        value.parent_id = it->get<std::string>();
    }
    value.resource_type = j.at("resource_type").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateRoleRequest& value) {
    j = nlohmann::json::object();
    j["description"] = value.description;
    j["is_global"] = value.is_global;
    j["name"] = value.name;
}

void from_json(const nlohmann::json& j, CreateRoleRequest& value) {
    value.description = j.at("description").get<std::string>();
    value.is_global = j.at("is_global").get<bool>();
    value.name = j.at("name").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateScimTokenRequest& value) {
    j = nlohmann::json::object();
    if (value.expires_in_days) {
        j["expires_in_days"] = *value.expires_in_days;
    }
    j["name"] = value.name;
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, CreateScimTokenRequest& value) {
    if (auto it = j.find("expires_in_days"); it != j.end() && !it->is_null()) {
        value.expires_in_days = it->get<std::int64_t>();
    }
    value.name = j.at("name").get<std::string>();
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateScimTokenResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["created_by"] = value.created_by;
    j["expires_at"] = value.expires_at;
    j["id"] = value.id;
    if (value.last_used_at) {
        j["last_used_at"] = *value.last_used_at;
    }
    j["name"] = value.name;
    if (value.revoked_at) {
        j["revoked_at"] = *value.revoked_at;
    }
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
    j["user_id"] = value.user_id;
    j["provisioning_token"] = detail::reveal(value.provisioning_token);
}

void from_json(const nlohmann::json& j, CreateScimTokenResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.created_by = j.at("created_by").get<std::string>();
    value.expires_at = j.at("expires_at").get<std::string>();
    value.id = j.at("id").get<std::string>();
    if (auto it = j.find("last_used_at"); it != j.end() && !it->is_null()) {
        value.last_used_at = it->get<std::string>();
    }
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("revoked_at"); it != j.end() && !it->is_null()) {
        value.revoked_at = it->get<std::string>();
    }
    value.status = scim_token_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.user_id = j.at("user_id").get<std::string>();
    value.provisioning_token = Sensitive<std::string>(j.at("provisioning_token").get<std::string>());
}

void to_json(nlohmann::json& j, const CreateScopeRequest& value) {
    j = nlohmann::json::object();
    j["description"] = value.description;
    j["name"] = value.name;
}

void from_json(const nlohmann::json& j, CreateScopeRequest& value) {
    value.description = j.at("description").get<std::string>();
    value.name = j.at("name").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateServiceAccountRequest& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    j["name"] = value.name;
}

void from_json(const nlohmann::json& j, CreateServiceAccountRequest& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    value.name = j.at("name").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateTenantRequest& value) {
    j = nlohmann::json::object();
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    j["name"] = value.name;
    j["slug"] = value.slug;
}

void from_json(const nlohmann::json& j, CreateTenantRequest& value) {
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    value.name = j.at("name").get<std::string>();
    value.slug = j.at("slug").get<std::string>();
}

void to_json(nlohmann::json& j, const OpaqueEnrollment& value) {
    j = nlohmann::json::object();
    j["opaque_session"] = value.opaque_session;
    j["registration_record"] = value.registration_record;
}

void from_json(const nlohmann::json& j, OpaqueEnrollment& value) {
    value.opaque_session = j.at("opaque_session").get<std::string>();
    value.registration_record = j.at("registration_record").get<std::string>();
}

void to_json(nlohmann::json& j, const CreateUserRequest& value) {
    j = nlohmann::json::object();
    j["email"] = value.email;
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    if (value.opaque) {
        j["opaque"] = *value.opaque;
    }
    j["password"] = detail::reveal(value.password);
    j["username"] = value.username;
}

void from_json(const nlohmann::json& j, CreateUserRequest& value) {
    value.email = j.at("email").get<std::string>();
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    if (auto it = j.find("opaque"); it != j.end() && !it->is_null()) {
        value.opaque = it->get<OpaqueEnrollment>();
    }
    value.password = Sensitive<std::string>(j.at("password").get<std::string>());
    value.username = j.at("username").get<std::string>();
}

void to_json(nlohmann::json& j, const RetryPolicy& value) {
    j = nlohmann::json::object();
    j["backoff_multiplier"] = value.backoff_multiplier;
    j["initial_delay_secs"] = value.initial_delay_secs;
    j["max_retries"] = value.max_retries;
}

void from_json(const nlohmann::json& j, RetryPolicy& value) {
    value.backoff_multiplier = j.at("backoff_multiplier").get<double>();
    value.initial_delay_secs = j.at("initial_delay_secs").get<std::int64_t>();
    value.max_retries = j.at("max_retries").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const CreateWebhookRequest& value) {
    j = nlohmann::json::object();
    j["events"] = value.events;
    if (value.retry_policy) {
        j["retry_policy"] = *value.retry_policy;
    }
    j["secret"] = detail::reveal(value.secret);
    j["url"] = value.url;
}

void from_json(const nlohmann::json& j, CreateWebhookRequest& value) {
    value.events = j.at("events").get<std::vector<std::string>>();
    if (auto it = j.find("retry_policy"); it != j.end() && !it->is_null()) {
        value.retry_policy = it->get<RetryPolicy>();
    }
    value.secret = Sensitive<std::string>(j.at("secret").get<std::string>());
    value.url = j.at("url").get<std::string>();
}

void to_json(nlohmann::json& j, const ProviderConfig& value) {
    // A union is forwarded EXACTLY as received. Re-encoding from the two members this SDK
    // models would drop every field belonging to the variant it does not model -- and the
    // server round-trips those.
    j = nlohmann::json::parse(value.raw, nullptr, false);
    if (j.is_discarded()) j = nlohmann::json::object();
}

void from_json(const nlohmann::json& j, ProviderConfig& value) {
    value.kind = j.at("kind").get<std::string>();
    value.raw = j.dump();
}

void to_json(nlohmann::json& j, const EmailConfig& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["enabled"] = value.enabled;
    j["from_email"] = value.from_email;
    j["from_name"] = value.from_name;
    j["id"] = value.id;
    j["provider"] = value.provider;
    if (value.reply_to) {
        j["reply_to"] = *value.reply_to;
    }
    j["scope"] = to_wire(value.scope);
    j["scope_id"] = value.scope_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, EmailConfig& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.enabled = j.at("enabled").get<bool>();
    value.from_email = j.at("from_email").get<std::string>();
    value.from_name = j.at("from_name").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.provider = j.at("provider").get<ProviderConfig>();
    if (auto it = j.find("reply_to"); it != j.end() && !it->is_null()) {
        value.reply_to = it->get<std::string>();
    }
    value.scope = settings_scope_from_wire(j.at("scope").get<std::string>());
    value.scope_id = j.at("scope_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const EmailConfigOverride& value) {
    j = nlohmann::json::object();
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    if (value.from_email) {
        j["from_email"] = *value.from_email;
    }
    if (value.from_name) {
        j["from_name"] = *value.from_name;
    }
    if (value.provider) {
        j["provider"] = *value.provider;
    }
    if (value.reply_to) {
        j["reply_to"] = *value.reply_to;
    }
}

void from_json(const nlohmann::json& j, EmailConfigOverride& value) {
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    if (auto it = j.find("from_email"); it != j.end() && !it->is_null()) {
        value.from_email = it->get<std::string>();
    }
    if (auto it = j.find("from_name"); it != j.end() && !it->is_null()) {
        value.from_name = it->get<std::string>();
    }
    if (auto it = j.find("provider"); it != j.end() && !it->is_null()) {
        value.provider = it->get<ProviderConfig>();
    }
    if (auto it = j.find("reply_to"); it != j.end() && !it->is_null()) {
        value.reply_to = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const EmailTestResult& value) {
    j = nlohmann::json::object();
    if (value.message_id) {
        j["message_id"] = *value.message_id;
    }
    j["provider"] = value.provider;
    j["to"] = value.to;
}

void from_json(const nlohmann::json& j, EmailTestResult& value) {
    if (auto it = j.find("message_id"); it != j.end() && !it->is_null()) {
        value.message_id = it->get<std::string>();
    }
    value.provider = j.at("provider").get<std::string>();
    value.to = j.at("to").get<std::string>();
}

void to_json(nlohmann::json& j, const EmailVerificationPolicy& value) {
    j = nlohmann::json::object();
    j["email_verification_grace_period_hours"] = value.email_verification_grace_period_hours;
    j["email_verification_required"] = value.email_verification_required;
}

void from_json(const nlohmann::json& j, EmailVerificationPolicy& value) {
    value.email_verification_grace_period_hours = j.at("email_verification_grace_period_hours").get<std::int64_t>();
    value.email_verification_required = j.at("email_verification_required").get<bool>();
}

void to_json(nlohmann::json& j, const EncryptRequest& value) {
    j = nlohmann::json::object();
    j["data_base64"] = value.data_base64;
}

void from_json(const nlohmann::json& j, EncryptRequest& value) {
    value.data_base64 = j.at("data_base64").get<std::string>();
}

void to_json(nlohmann::json& j, const EncryptedExport& value) {
    j = nlohmann::json::object();
    j["ciphertext_armored"] = value.ciphertext_armored;
    j["recipient_key_id"] = value.recipient_key_id;
}

void from_json(const nlohmann::json& j, EncryptedExport& value) {
    value.ciphertext_armored = j.at("ciphertext_armored").get<std::string>();
    value.recipient_key_id = j.at("recipient_key_id").get<std::string>();
}

void to_json(nlohmann::json& j, const TokenExchangeTrustResponse& value) {
    j = nlohmann::json::object();
    j["accepted_audiences"] = value.accepted_audiences;
    j["enabled"] = value.enabled;
    if (value.max_lifetime_secs) {
        j["max_lifetime_secs"] = *value.max_lifetime_secs;
    }
    j["max_token_age_secs"] = value.max_token_age_secs;
    { auto parsed = nlohmann::json::parse(value.scope_map, nullptr, false); if (!parsed.is_discarded()) j["scope_map"] = parsed; }
    j["subject_mapping"] = value.subject_mapping;
}

void from_json(const nlohmann::json& j, TokenExchangeTrustResponse& value) {
    value.accepted_audiences = j.at("accepted_audiences").get<std::vector<std::string>>();
    value.enabled = j.at("enabled").get<bool>();
    if (auto it = j.find("max_lifetime_secs"); it != j.end() && !it->is_null()) {
        value.max_lifetime_secs = it->get<std::int64_t>();
    }
    value.max_token_age_secs = j.at("max_token_age_secs").get<std::int64_t>();
    value.scope_map = j.at("scope_map").dump();
    value.subject_mapping = j.at("subject_mapping").get<std::string>();
}

void to_json(nlohmann::json& j, const FederationConfigResponse& value) {
    j = nlohmann::json::object();
    { auto parsed = nlohmann::json::parse(value.attribute_map, nullptr, false); if (!parsed.is_discarded()) j["attribute_map"] = parsed; }
    j["client_id"] = value.client_id;
    j["created_at"] = value.created_at;
    j["enabled"] = value.enabled;
    j["id"] = value.id;
    if (value.metadata_url) {
        j["metadata_url"] = *value.metadata_url;
    }
    j["protocol"] = value.protocol;
    j["provider"] = value.provider;
    j["tenant_id"] = value.tenant_id;
    j["token_exchange"] = value.token_exchange;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, FederationConfigResponse& value) {
    value.attribute_map = j.at("attribute_map").dump();
    value.client_id = j.at("client_id").get<std::string>();
    value.created_at = j.at("created_at").get<std::string>();
    value.enabled = j.at("enabled").get<bool>();
    value.id = j.at("id").get<std::string>();
    if (auto it = j.find("metadata_url"); it != j.end() && !it->is_null()) {
        value.metadata_url = it->get<std::string>();
    }
    value.protocol = j.at("protocol").get<std::string>();
    value.provider = j.at("provider").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.token_exchange = j.at("token_exchange").get<TokenExchangeTrustResponse>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const FederationLinkResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    if (value.external_email) {
        j["external_email"] = *value.external_email;
    }
    j["external_subject"] = value.external_subject;
    j["federation_config_id"] = value.federation_config_id;
    j["id"] = value.id;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, FederationLinkResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    if (auto it = j.find("external_email"); it != j.end() && !it->is_null()) {
        value.external_email = it->get<std::string>();
    }
    value.external_subject = j.at("external_subject").get<std::string>();
    value.federation_config_id = j.at("federation_config_id").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const GeneratedCaCertificate& value) {
    j = nlohmann::json::object();
    if (value.chain_pem) {
        j["chain_pem"] = *value.chain_pem;
    }
    j["created_at"] = value.created_at;
    j["fingerprint"] = value.fingerprint;
    j["id"] = value.id;
    j["key_algorithm"] = to_wire(value.key_algorithm);
    if (value.key_custody) {
        j["key_custody"] = *value.key_custody;
    }
    if (value.key_locator) {
        j["key_locator"] = *value.key_locator;
    }
    if (value.mtls_trust_anchor) {
        j["mtls_trust_anchor"] = *value.mtls_trust_anchor;
    }
    j["not_after"] = value.not_after;
    j["not_before"] = value.not_before;
    j["organization_id"] = value.organization_id;
    if (value.parent_ca_id) {
        j["parent_ca_id"] = *value.parent_ca_id;
    }
    j["public_cert_pem"] = value.public_cert_pem;
    j["status"] = to_wire(value.status);
    j["subject"] = value.subject;
    if (value.tenant_id) {
        j["tenant_id"] = *value.tenant_id;
    }
    if (value.private_key_pem) {
        j["private_key_pem"] = detail::reveal(*value.private_key_pem);
    }
}

void from_json(const nlohmann::json& j, GeneratedCaCertificate& value) {
    if (auto it = j.find("chain_pem"); it != j.end() && !it->is_null()) {
        value.chain_pem = it->get<std::string>();
    }
    value.created_at = j.at("created_at").get<std::string>();
    value.fingerprint = j.at("fingerprint").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    if (auto it = j.find("key_custody"); it != j.end() && !it->is_null()) {
        value.key_custody = it->get<std::string>();
    }
    if (auto it = j.find("key_locator"); it != j.end() && !it->is_null()) {
        value.key_locator = it->get<std::string>();
    }
    if (auto it = j.find("mtls_trust_anchor"); it != j.end() && !it->is_null()) {
        value.mtls_trust_anchor = it->get<bool>();
    }
    value.not_after = j.at("not_after").get<std::string>();
    value.not_before = j.at("not_before").get<std::string>();
    value.organization_id = j.at("organization_id").get<std::string>();
    if (auto it = j.find("parent_ca_id"); it != j.end() && !it->is_null()) {
        value.parent_ca_id = it->get<std::string>();
    }
    value.public_cert_pem = j.at("public_cert_pem").get<std::string>();
    value.status = certificate_status_from_wire(j.at("status").get<std::string>());
    value.subject = j.at("subject").get<std::string>();
    if (auto it = j.find("tenant_id"); it != j.end() && !it->is_null()) {
        value.tenant_id = it->get<std::string>();
    }
    if (auto it = j.find("private_key_pem"); it != j.end() && !it->is_null()) {
        value.private_key_pem = Sensitive<std::string>(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const GeneratedCertificate& value) {
    j = nlohmann::json::object();
    j["cert_type"] = to_wire(value.cert_type);
    j["created_at"] = value.created_at;
    j["fingerprint"] = value.fingerprint;
    j["id"] = value.id;
    j["issuer_ca_id"] = value.issuer_ca_id;
    j["key_algorithm"] = to_wire(value.key_algorithm);
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["not_after"] = value.not_after;
    j["not_before"] = value.not_before;
    j["public_cert_pem"] = value.public_cert_pem;
    j["status"] = to_wire(value.status);
    j["subject"] = value.subject;
    j["tenant_id"] = value.tenant_id;
    if (value.chain_pem) {
        j["chain_pem"] = *value.chain_pem;
    }
    j["private_key_pem"] = detail::reveal(value.private_key_pem);
}

void from_json(const nlohmann::json& j, GeneratedCertificate& value) {
    value.cert_type = certificate_type_from_wire(j.at("cert_type").get<std::string>());
    value.created_at = j.at("created_at").get<std::string>();
    value.fingerprint = j.at("fingerprint").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.issuer_ca_id = j.at("issuer_ca_id").get<std::string>();
    value.key_algorithm = key_algorithm_from_wire(j.at("key_algorithm").get<std::string>());
    value.metadata = j.at("metadata").dump();
    value.not_after = j.at("not_after").get<std::string>();
    value.not_before = j.at("not_before").get<std::string>();
    value.public_cert_pem = j.at("public_cert_pem").get<std::string>();
    value.status = certificate_status_from_wire(j.at("status").get<std::string>());
    value.subject = j.at("subject").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    if (auto it = j.find("chain_pem"); it != j.end() && !it->is_null()) {
        value.chain_pem = it->get<std::string>();
    }
    value.private_key_pem = Sensitive<std::string>(j.at("private_key_pem").get<std::string>());
}

void to_json(nlohmann::json& j, const GeneratedPgpKey& value) {
    j = nlohmann::json::object();
    j["algorithm"] = to_wire(value.algorithm);
    j["created_at"] = value.created_at;
    j["fingerprint"] = value.fingerprint;
    j["id"] = value.id;
    j["name"] = value.name;
    j["public_key_armored"] = value.public_key_armored;
    j["purpose"] = to_wire(value.purpose);
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
    if (value.private_key_armored) {
        j["private_key_armored"] = detail::reveal(*value.private_key_armored);
    }
}

void from_json(const nlohmann::json& j, GeneratedPgpKey& value) {
    value.algorithm = pgp_key_algorithm_from_wire(j.at("algorithm").get<std::string>());
    value.created_at = j.at("created_at").get<std::string>();
    value.fingerprint = j.at("fingerprint").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.public_key_armored = j.at("public_key_armored").get<std::string>();
    value.purpose = pgp_key_purpose_from_wire(j.at("purpose").get<std::string>());
    value.status = pgp_key_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
    if (auto it = j.find("private_key_armored"); it != j.end() && !it->is_null()) {
        value.private_key_armored = Sensitive<std::string>(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const GrantPermissionRequest& value) {
    j = nlohmann::json::object();
    if (value.effect) {
        j["effect"] = to_wire(*value.effect);
    }
    j["permission_id"] = value.permission_id;
    if (value.scope_ids) {
        j["scope_ids"] = *value.scope_ids;
    }
}

void from_json(const nlohmann::json& j, GrantPermissionRequest& value) {
    if (auto it = j.find("effect"); it != j.end() && !it->is_null()) {
        value.effect = permission_effect_from_wire(it->get<std::string>());
    }
    value.permission_id = j.at("permission_id").get<std::string>();
    if (auto it = j.find("scope_ids"); it != j.end() && !it->is_null()) {
        value.scope_ids = it->get<std::vector<std::string>>();
    }
}

void to_json(nlohmann::json& j, const GrantedScope& value) {
    j = nlohmann::json::object();
    j["id"] = value.id;
    j["name"] = value.name;
    j["resource_id"] = value.resource_id;
}

void from_json(const nlohmann::json& j, GrantedScope& value) {
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.resource_id = j.at("resource_id").get<std::string>();
}

void to_json(nlohmann::json& j, const Group& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["description"] = value.description;
    j["id"] = value.id;
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["name"] = value.name;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Group& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.description = j.at("description").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.metadata = j.at("metadata").dump();
    value.name = j.at("name").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const HealthResponse& value) {
    j = nlohmann::json::object();
    j["status"] = value.status;
}

void from_json(const nlohmann::json& j, HealthResponse& value) {
    value.status = j.at("status").get<std::string>();
}

void to_json(nlohmann::json& j, const ImportCaCertificateRequest& value) {
    j = nlohmann::json::object();
    if (value.private_key_pem) {
        j["private_key_pem"] = detail::reveal(*value.private_key_pem);
    }
    j["public_cert_pem"] = value.public_cert_pem;
}

void from_json(const nlohmann::json& j, ImportCaCertificateRequest& value) {
    if (auto it = j.find("private_key_pem"); it != j.end() && !it->is_null()) {
        value.private_key_pem = Sensitive<std::string>(it->get<std::string>());
    }
    value.public_cert_pem = j.at("public_cert_pem").get<std::string>();
}

void to_json(nlohmann::json& j, const LockoutPolicy& value) {
    j = nlohmann::json::object();
    j["lockout_backoff_multiplier"] = value.lockout_backoff_multiplier;
    j["lockout_duration_secs"] = value.lockout_duration_secs;
    j["max_failed_login_attempts"] = value.max_failed_login_attempts;
    j["max_lockout_duration_secs"] = value.max_lockout_duration_secs;
}

void from_json(const nlohmann::json& j, LockoutPolicy& value) {
    value.lockout_backoff_multiplier = j.at("lockout_backoff_multiplier").get<double>();
    value.lockout_duration_secs = j.at("lockout_duration_secs").get<std::int64_t>();
    value.max_failed_login_attempts = j.at("max_failed_login_attempts").get<std::int64_t>();
    value.max_lockout_duration_secs = j.at("max_lockout_duration_secs").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const MdsRefreshOutcome& value) {
    // A union is forwarded EXACTLY as received. Re-encoding from the two members this SDK
    // models would drop every field belonging to the variant it does not model -- and the
    // server round-trips those.
    j = nlohmann::json::parse(value.raw, nullptr, false);
    if (j.is_discarded()) j = nlohmann::json::object();
}

void from_json(const nlohmann::json& j, MdsRefreshOutcome& value) {
    value.outcome = j.at("outcome").get<std::string>();
    value.raw = j.dump();
}

void to_json(nlohmann::json& j, const MdsStatusResponse& value) {
    j = nlohmann::json::object();
    j["entry_count"] = value.entry_count;
    if (value.last_refreshed_at) {
        j["last_refreshed_at"] = *value.last_refreshed_at;
    }
    if (value.next_update) {
        j["next_update"] = *value.next_update;
    }
    if (value.no) {
        j["no"] = *value.no;
    }
    j["stale"] = value.stale;
}

void from_json(const nlohmann::json& j, MdsStatusResponse& value) {
    value.entry_count = j.at("entry_count").get<std::int64_t>();
    if (auto it = j.find("last_refreshed_at"); it != j.end() && !it->is_null()) {
        value.last_refreshed_at = it->get<std::string>();
    }
    if (auto it = j.find("next_update"); it != j.end() && !it->is_null()) {
        value.next_update = it->get<std::string>();
    }
    if (auto it = j.find("no"); it != j.end() && !it->is_null()) {
        value.no = it->get<std::int64_t>();
    }
    value.stale = j.at("stale").get<bool>();
}

void to_json(nlohmann::json& j, const MfaMethodResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    if (value.last_used_at) {
        j["last_used_at"] = *value.last_used_at;
    }
    j["method_id"] = value.method_id;
    j["method_type"] = to_wire(value.method_type);
    j["name"] = value.name;
}

void from_json(const nlohmann::json& j, MfaMethodResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    if (auto it = j.find("last_used_at"); it != j.end() && !it->is_null()) {
        value.last_used_at = it->get<std::string>();
    }
    value.method_id = j.at("method_id").get<std::string>();
    value.method_type = mfa_method_type_from_wire(j.at("method_type").get<std::string>());
    value.name = j.at("name").get<std::string>();
}

void to_json(nlohmann::json& j, const MfaPolicy& value) {
    j = nlohmann::json::object();
    j["mfa_challenge_lifetime_secs"] = value.mfa_challenge_lifetime_secs;
    j["mfa_enforced"] = value.mfa_enforced;
}

void from_json(const nlohmann::json& j, MfaPolicy& value) {
    value.mfa_challenge_lifetime_secs = j.at("mfa_challenge_lifetime_secs").get<std::int64_t>();
    value.mfa_enforced = j.at("mfa_enforced").get<bool>();
}

void to_json(nlohmann::json& j, const MigrateCustodyResponse& value) {
    j = nlohmann::json::object();
    j["ca_certificate_id"] = value.ca_certificate_id;
    j["key_custody"] = value.key_custody;
    if (value.key_locator) {
        j["key_locator"] = *value.key_locator;
    }
    j["previous_custody"] = value.previous_custody;
}

void from_json(const nlohmann::json& j, MigrateCustodyResponse& value) {
    value.ca_certificate_id = j.at("ca_certificate_id").get<std::string>();
    value.key_custody = j.at("key_custody").get<std::string>();
    if (auto it = j.find("key_locator"); it != j.end() && !it->is_null()) {
        value.key_locator = it->get<std::string>();
    }
    value.previous_custody = j.at("previous_custody").get<std::string>();
}

void to_json(nlohmann::json& j, const MtlsTrustAnchorResponse& value) {
    j = nlohmann::json::object();
    j["ca_certificate_id"] = value.ca_certificate_id;
    j["message"] = value.message;
    j["mtls_trust_anchor"] = value.mtls_trust_anchor;
    j["restart_required"] = value.restart_required;
}

void from_json(const nlohmann::json& j, MtlsTrustAnchorResponse& value) {
    value.ca_certificate_id = j.at("ca_certificate_id").get<std::string>();
    value.message = j.at("message").get<std::string>();
    value.mtls_trust_anchor = j.at("mtls_trust_anchor").get<bool>();
    value.restart_required = j.at("restart_required").get<bool>();
}

void to_json(nlohmann::json& j, const NotificationPolicy& value) {
    j = nlohmann::json::object();
    j["admin_notifications_enabled"] = value.admin_notifications_enabled;
}

void from_json(const nlohmann::json& j, NotificationPolicy& value) {
    value.admin_notifications_enabled = j.at("admin_notifications_enabled").get<bool>();
}

void to_json(nlohmann::json& j, const NotificationRuleResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["description"] = value.description;
    j["enabled"] = value.enabled;
    j["events"] = value.events;
    j["id"] = value.id;
    j["name"] = value.name;
    j["recipient_emails"] = value.recipient_emails;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, NotificationRuleResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.description = j.at("description").get<std::string>();
    value.enabled = j.at("enabled").get<bool>();
    value.events = j.at("events").get<std::vector<NotificationEventType>>();
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.recipient_emails = j.at("recipient_emails").get<std::vector<std::string>>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const OAuth2ClientCreatedResponse& value) {
    j = nlohmann::json::object();
    j["client_id"] = value.client_id;
    j["client_secret"] = detail::reveal(value.client_secret);
    j["created_at"] = value.created_at;
    j["grant_types"] = value.grant_types;
    j["id"] = value.id;
    j["name"] = value.name;
    j["redirect_uris"] = value.redirect_uris;
    j["scopes"] = value.scopes;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, OAuth2ClientCreatedResponse& value) {
    value.client_id = j.at("client_id").get<std::string>();
    value.client_secret = Sensitive<std::string>(j.at("client_secret").get<std::string>());
    value.created_at = j.at("created_at").get<std::string>();
    value.grant_types = j.at("grant_types").get<std::vector<std::string>>();
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.redirect_uris = j.at("redirect_uris").get<std::vector<std::string>>();
    value.scopes = j.at("scopes").get<std::vector<std::string>>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const OAuth2ClientResponse& value) {
    j = nlohmann::json::object();
    j["client_id"] = value.client_id;
    j["created_at"] = value.created_at;
    j["dpop_bound_access_tokens"] = value.dpop_bound_access_tokens;
    j["dpop_require_nonce"] = value.dpop_require_nonce;
    j["grant_types"] = value.grant_types;
    j["id"] = value.id;
    if (value.jwks) {
        j["jwks"] = *value.jwks;
    }
    if (value.jwks_uri) {
        j["jwks_uri"] = *value.jwks_uri;
    }
    j["name"] = value.name;
    j["profile"] = to_wire(value.profile);
    j["redirect_uris"] = value.redirect_uris;
    j["require_par"] = value.require_par;
    j["scopes"] = value.scopes;
    j["self_signed_tls_client_auth_thumbprints"] = value.self_signed_tls_client_auth_thumbprints;
    j["tenant_id"] = value.tenant_id;
    if (value.tls_client_auth_san_dns) {
        j["tls_client_auth_san_dns"] = *value.tls_client_auth_san_dns;
    }
    if (value.tls_client_auth_san_uri) {
        j["tls_client_auth_san_uri"] = *value.tls_client_auth_san_uri;
    }
    if (value.tls_client_auth_subject_dn) {
        j["tls_client_auth_subject_dn"] = *value.tls_client_auth_subject_dn;
    }
    j["tls_client_certificate_bound_access_tokens"] = value.tls_client_certificate_bound_access_tokens;
    j["token_endpoint_auth_method"] = to_wire(value.token_endpoint_auth_method);
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, OAuth2ClientResponse& value) {
    value.client_id = j.at("client_id").get<std::string>();
    value.created_at = j.at("created_at").get<std::string>();
    value.dpop_bound_access_tokens = j.at("dpop_bound_access_tokens").get<bool>();
    value.dpop_require_nonce = j.at("dpop_require_nonce").get<bool>();
    value.grant_types = j.at("grant_types").get<std::vector<std::string>>();
    value.id = j.at("id").get<std::string>();
    if (auto it = j.find("jwks"); it != j.end() && !it->is_null()) {
        value.jwks = it->get<std::string>();
    }
    if (auto it = j.find("jwks_uri"); it != j.end() && !it->is_null()) {
        value.jwks_uri = it->get<std::string>();
    }
    value.name = j.at("name").get<std::string>();
    value.profile = client_profile_from_wire(j.at("profile").get<std::string>());
    value.redirect_uris = j.at("redirect_uris").get<std::vector<std::string>>();
    value.require_par = j.at("require_par").get<bool>();
    value.scopes = j.at("scopes").get<std::vector<std::string>>();
    value.self_signed_tls_client_auth_thumbprints = j.at("self_signed_tls_client_auth_thumbprints").get<std::vector<std::string>>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    if (auto it = j.find("tls_client_auth_san_dns"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_san_dns = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_auth_san_uri"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_san_uri = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_auth_subject_dn"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_subject_dn = it->get<std::string>();
    }
    value.tls_client_certificate_bound_access_tokens = j.at("tls_client_certificate_bound_access_tokens").get<bool>();
    value.token_endpoint_auth_method = client_auth_method_from_wire(j.at("token_endpoint_auth_method").get<std::string>());
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const OidcAuthorizeRequest& value) {
    j = nlohmann::json::object();
    j["config_id"] = value.config_id;
    j["nonce"] = value.nonce;
    j["redirect_uri"] = value.redirect_uri;
    j["state"] = value.state;
}

void from_json(const nlohmann::json& j, OidcAuthorizeRequest& value) {
    value.config_id = j.at("config_id").get<std::string>();
    value.nonce = j.at("nonce").get<std::string>();
    value.redirect_uri = j.at("redirect_uri").get<std::string>();
    value.state = j.at("state").get<std::string>();
}

void to_json(nlohmann::json& j, const OidcAuthorizeResponse& value) {
    j = nlohmann::json::object();
    j["url"] = value.url;
}

void from_json(const nlohmann::json& j, OidcAuthorizeResponse& value) {
    value.url = j.at("url").get<std::string>();
}

void to_json(nlohmann::json& j, const OidcCallbackRequest& value) {
    j = nlohmann::json::object();
    j["code"] = value.code;
    j["config_id"] = value.config_id;
    j["nonce"] = value.nonce;
    j["redirect_uri"] = value.redirect_uri;
    j["state"] = value.state;
}

void from_json(const nlohmann::json& j, OidcCallbackRequest& value) {
    value.code = j.at("code").get<std::string>();
    value.config_id = j.at("config_id").get<std::string>();
    value.nonce = j.at("nonce").get<std::string>();
    value.redirect_uri = j.at("redirect_uri").get<std::string>();
    value.state = j.at("state").get<std::string>();
}

void to_json(nlohmann::json& j, const OidcCallbackResponse& value) {
    j = nlohmann::json::object();
    j["federation_link_id"] = value.federation_link_id;
    j["newly_provisioned"] = value.newly_provisioned;
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, OidcCallbackResponse& value) {
    value.federation_link_id = j.at("federation_link_id").get<std::string>();
    value.newly_provisioned = j.at("newly_provisioned").get<bool>();
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const OpaquePolicy& value) {
    j = nlohmann::json::object();
    j["opaque_ksf"] = value.opaque_ksf;
    j["opaque_mode"] = value.opaque_mode;
    j["opaque_suite"] = value.opaque_suite;
}

void from_json(const nlohmann::json& j, OpaquePolicy& value) {
    value.opaque_ksf = j.at("opaque_ksf").get<std::string>();
    value.opaque_mode = j.at("opaque_mode").get<std::string>();
    value.opaque_suite = j.at("opaque_suite").get<std::string>();
}

void to_json(nlohmann::json& j, const Organization& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["id"] = value.id;
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["name"] = value.name;
    j["slug"] = value.slug;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Organization& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.metadata = j.at("metadata").dump();
    value.name = j.at("name").get<std::string>();
    value.slug = j.at("slug").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const PasswordPolicy& value) {
    j = nlohmann::json::object();
    j["hibp_check_enabled"] = value.hibp_check_enabled;
    j["min_length"] = value.min_length;
    j["password_history_count"] = value.password_history_count;
    j["require_digits"] = value.require_digits;
    j["require_lowercase"] = value.require_lowercase;
    j["require_symbols"] = value.require_symbols;
    j["require_uppercase"] = value.require_uppercase;
}

void from_json(const nlohmann::json& j, PasswordPolicy& value) {
    value.hibp_check_enabled = j.at("hibp_check_enabled").get<bool>();
    value.min_length = j.at("min_length").get<std::int64_t>();
    value.password_history_count = j.at("password_history_count").get<std::int64_t>();
    value.require_digits = j.at("require_digits").get<bool>();
    value.require_lowercase = j.at("require_lowercase").get<bool>();
    value.require_symbols = j.at("require_symbols").get<bool>();
    value.require_uppercase = j.at("require_uppercase").get<bool>();
}

void to_json(nlohmann::json& j, const Permission& value) {
    j = nlohmann::json::object();
    j["action"] = value.action;
    j["created_at"] = value.created_at;
    j["description"] = value.description;
    j["id"] = value.id;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Permission& value) {
    value.action = j.at("action").get<std::string>();
    value.created_at = j.at("created_at").get<std::string>();
    value.description = j.at("description").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const PgpKey& value) {
    j = nlohmann::json::object();
    j["algorithm"] = to_wire(value.algorithm);
    j["created_at"] = value.created_at;
    j["fingerprint"] = value.fingerprint;
    j["id"] = value.id;
    j["name"] = value.name;
    j["public_key_armored"] = value.public_key_armored;
    j["purpose"] = to_wire(value.purpose);
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
}

void from_json(const nlohmann::json& j, PgpKey& value) {
    value.algorithm = pgp_key_algorithm_from_wire(j.at("algorithm").get<std::string>());
    value.created_at = j.at("created_at").get<std::string>();
    value.fingerprint = j.at("fingerprint").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.public_key_armored = j.at("public_key_armored").get<std::string>();
    value.purpose = pgp_key_purpose_from_wire(j.at("purpose").get<std::string>());
    value.status = pgp_key_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
}

void to_json(nlohmann::json& j, const PolicyResponse& value) {
    j = nlohmann::json::object();
    if (value.allowed_aaguids) {
        j["allowed_aaguids"] = *value.allowed_aaguids;
    }
    j["block_revoked_status"] = value.block_revoked_status;
    if (value.blocked_aaguids) {
        j["blocked_aaguids"] = *value.blocked_aaguids;
    }
    if (value.min_certification) {
        j["min_certification"] = to_wire(*value.min_certification);
    }
    j["mode"] = to_wire(value.mode);
    j["require_fido_certified"] = value.require_fido_certified;
    if (value.unknown_aaguid) {
        j["unknown_aaguid"] = to_wire(*value.unknown_aaguid);
    }
    j["effective_unknown_aaguid"] = to_wire(value.effective_unknown_aaguid);
}

void from_json(const nlohmann::json& j, PolicyResponse& value) {
    if (auto it = j.find("allowed_aaguids"); it != j.end() && !it->is_null()) {
        value.allowed_aaguids = it->get<std::vector<std::string>>();
    }
    value.block_revoked_status = j.at("block_revoked_status").get<bool>();
    if (auto it = j.find("blocked_aaguids"); it != j.end() && !it->is_null()) {
        value.blocked_aaguids = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("min_certification"); it != j.end() && !it->is_null()) {
        value.min_certification = certification_level_from_wire(it->get<std::string>());
    }
    value.mode = attestation_mode_from_wire(j.at("mode").get<std::string>());
    value.require_fido_certified = j.at("require_fido_certified").get<bool>();
    if (auto it = j.find("unknown_aaguid"); it != j.end() && !it->is_null()) {
        value.unknown_aaguid = unknown_aaguid_action_from_wire(it->get<std::string>());
    }
    value.effective_unknown_aaguid = unknown_aaguid_action_from_wire(j.at("effective_unknown_aaguid").get<std::string>());
}

void to_json(nlohmann::json& j, const PrivacyPolicy& value) {
    j = nlohmann::json::object();
    j["deletion_grace_period_days"] = value.deletion_grace_period_days;
}

void from_json(const nlohmann::json& j, PrivacyPolicy& value) {
    value.deletion_grace_period_days = j.at("deletion_grace_period_days").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const ReactorEventDescriptor& value) {
    j = nlohmann::json::object();
    j["default_failure_policy"] = to_wire(value.default_failure_policy);
    j["description"] = value.description;
    j["interceptable"] = value.interceptable;
    j["mutable"] = value.mutable_;
    j["mutable_fields"] = value.mutable_fields;
    j["name"] = value.name;
}

void from_json(const nlohmann::json& j, ReactorEventDescriptor& value) {
    value.default_failure_policy = failure_policy_from_wire(j.at("default_failure_policy").get<std::string>());
    value.description = j.at("description").get<std::string>();
    value.interceptable = j.at("interceptable").get<bool>();
    value.mutable_ = j.at("mutable").get<bool>();
    value.mutable_fields = j.at("mutable_fields").get<std::vector<std::string>>();
    value.name = j.at("name").get<std::string>();
}

void to_json(nlohmann::json& j, const ReactorResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["description"] = value.description;
    j["enabled"] = value.enabled;
    j["events"] = value.events;
    j["failure_policy"] = to_wire(value.failure_policy);
    j["id"] = value.id;
    if (value.last_seen_at) {
        j["last_seen_at"] = *value.last_seen_at;
    }
    j["mode"] = to_wire(value.mode);
    j["name"] = value.name;
    j["priority"] = value.priority;
    j["recent_timeout_count"] = value.recent_timeout_count;
    j["recent_veto_count"] = value.recent_veto_count;
    j["tenant_id"] = value.tenant_id;
    j["timeout_ms"] = value.timeout_ms;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, ReactorResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.description = j.at("description").get<std::string>();
    value.enabled = j.at("enabled").get<bool>();
    value.events = j.at("events").get<std::vector<std::string>>();
    value.failure_policy = failure_policy_from_wire(j.at("failure_policy").get<std::string>());
    value.id = j.at("id").get<std::string>();
    if (auto it = j.find("last_seen_at"); it != j.end() && !it->is_null()) {
        value.last_seen_at = it->get<std::string>();
    }
    value.mode = reactor_mode_from_wire(j.at("mode").get<std::string>());
    value.name = j.at("name").get<std::string>();
    value.priority = j.at("priority").get<std::int64_t>();
    value.recent_timeout_count = j.at("recent_timeout_count").get<std::int64_t>();
    value.recent_veto_count = j.at("recent_veto_count").get<std::int64_t>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.timeout_ms = j.at("timeout_ms").get<std::int64_t>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const ReadyResponse& value) {
    j = nlohmann::json::object();
    j["database"] = value.database;
    j["status"] = value.status;
}

void from_json(const nlohmann::json& j, ReadyResponse& value) {
    value.database = j.at("database").get<std::string>();
    value.status = j.at("status").get<std::string>();
}

void to_json(nlohmann::json& j, const ResolvedPermissionGrant& value) {
    j = nlohmann::json::object();
    j["effect"] = to_wire(value.effect);
    j["permission"] = value.permission;
    j["scope_ids"] = value.scope_ids;
    j["scopes"] = value.scopes;
}

void from_json(const nlohmann::json& j, ResolvedPermissionGrant& value) {
    value.effect = permission_effect_from_wire(j.at("effect").get<std::string>());
    value.permission = j.at("permission").get<Permission>();
    value.scope_ids = j.at("scope_ids").get<std::vector<std::string>>();
    value.scopes = j.at("scopes").get<std::vector<GrantedScope>>();
}

void to_json(nlohmann::json& j, const Resource& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["id"] = value.id;
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["name"] = value.name;
    if (value.parent_id) {
        j["parent_id"] = *value.parent_id;
    }
    j["resource_type"] = value.resource_type;
    j["tenant_id"] = value.tenant_id;
    if (value.uma_registered_by) {
        j["uma_registered_by"] = *value.uma_registered_by;
    }
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Resource& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.metadata = j.at("metadata").dump();
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("parent_id"); it != j.end() && !it->is_null()) {
        value.parent_id = it->get<std::string>();
    }
    value.resource_type = j.at("resource_type").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    if (auto it = j.find("uma_registered_by"); it != j.end() && !it->is_null()) {
        value.uma_registered_by = it->get<std::string>();
    }
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const Role& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["description"] = value.description;
    j["id"] = value.id;
    j["is_global"] = value.is_global;
    j["name"] = value.name;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Role& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.description = j.at("description").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.is_global = j.at("is_global").get<bool>();
    value.name = j.at("name").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const RoleAssignment& value) {
    j = nlohmann::json::object();
    if (value.resource_id) {
        j["resource_id"] = *value.resource_id;
    }
    j["role"] = value.role;
}

void from_json(const nlohmann::json& j, RoleAssignment& value) {
    if (auto it = j.find("resource_id"); it != j.end() && !it->is_null()) {
        value.resource_id = it->get<std::string>();
    }
    value.role = j.at("role").get<Role>();
}

void to_json(nlohmann::json& j, const RoleGroupAssignment& value) {
    j = nlohmann::json::object();
    j["group"] = value.group;
    if (value.resource_id) {
        j["resource_id"] = *value.resource_id;
    }
}

void from_json(const nlohmann::json& j, RoleGroupAssignment& value) {
    value.group = j.at("group").get<Group>();
    if (auto it = j.find("resource_id"); it != j.end() && !it->is_null()) {
        value.resource_id = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UserResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["email"] = value.email;
    j["email_verified"] = value.email_verified;
    j["failed_login_attempts"] = value.failed_login_attempts;
    j["id"] = value.id;
    j["is_locked"] = value.is_locked;
    if (value.locked_until) {
        j["locked_until"] = *value.locked_until;
    }
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["mfa_enabled"] = value.mfa_enabled;
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
    j["username"] = value.username;
}

void from_json(const nlohmann::json& j, UserResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.email = j.at("email").get<std::string>();
    value.email_verified = j.at("email_verified").get<bool>();
    value.failed_login_attempts = j.at("failed_login_attempts").get<std::int64_t>();
    value.id = j.at("id").get<std::string>();
    value.is_locked = j.at("is_locked").get<bool>();
    if (auto it = j.find("locked_until"); it != j.end() && !it->is_null()) {
        value.locked_until = it->get<std::string>();
    }
    value.metadata = j.at("metadata").dump();
    value.mfa_enabled = j.at("mfa_enabled").get<bool>();
    value.status = user_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
    value.username = j.at("username").get<std::string>();
}

void to_json(nlohmann::json& j, const RoleUserAssignment& value) {
    j = nlohmann::json::object();
    if (value.resource_id) {
        j["resource_id"] = *value.resource_id;
    }
    j["user"] = value.user;
}

void from_json(const nlohmann::json& j, RoleUserAssignment& value) {
    if (auto it = j.find("resource_id"); it != j.end() && !it->is_null()) {
        value.resource_id = it->get<std::string>();
    }
    value.user = j.at("user").get<UserResponse>();
}

void to_json(nlohmann::json& j, const RotateSecretResponse& value) {
    j = nlohmann::json::object();
    j["client_secret"] = detail::reveal(value.client_secret);
}

void from_json(const nlohmann::json& j, RotateSecretResponse& value) {
    value.client_secret = Sensitive<std::string>(j.at("client_secret").get<std::string>());
}

void to_json(nlohmann::json& j, const ScimTokenResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["created_by"] = value.created_by;
    j["expires_at"] = value.expires_at;
    j["id"] = value.id;
    if (value.last_used_at) {
        j["last_used_at"] = *value.last_used_at;
    }
    j["name"] = value.name;
    if (value.revoked_at) {
        j["revoked_at"] = *value.revoked_at;
    }
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
    j["user_id"] = value.user_id;
}

void from_json(const nlohmann::json& j, ScimTokenResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.created_by = j.at("created_by").get<std::string>();
    value.expires_at = j.at("expires_at").get<std::string>();
    value.id = j.at("id").get<std::string>();
    if (auto it = j.find("last_used_at"); it != j.end() && !it->is_null()) {
        value.last_used_at = it->get<std::string>();
    }
    value.name = j.at("name").get<std::string>();
    if (auto it = j.find("revoked_at"); it != j.end() && !it->is_null()) {
        value.revoked_at = it->get<std::string>();
    }
    value.status = scim_token_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.user_id = j.at("user_id").get<std::string>();
}

void to_json(nlohmann::json& j, const Scope& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["description"] = value.description;
    j["id"] = value.id;
    j["name"] = value.name;
    j["resource_id"] = value.resource_id;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Scope& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.description = j.at("description").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.resource_id = j.at("resource_id").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const TokenPolicy& value) {
    j = nlohmann::json::object();
    j["access_token_lifetime_secs"] = value.access_token_lifetime_secs;
    j["refresh_token_lifetime_secs"] = value.refresh_token_lifetime_secs;
}

void from_json(const nlohmann::json& j, TokenPolicy& value) {
    value.access_token_lifetime_secs = j.at("access_token_lifetime_secs").get<std::int64_t>();
    value.refresh_token_lifetime_secs = j.at("refresh_token_lifetime_secs").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const SecuritySettings& value) {
    j = nlohmann::json::object();
    j["certificate"] = value.certificate;
    j["created_at"] = value.created_at;
    j["email"] = value.email;
    j["id"] = value.id;
    j["lockout"] = value.lockout;
    j["mfa"] = value.mfa;
    j["notification"] = value.notification;
    j["opaque"] = value.opaque;
    j["password"] = value.password;
    j["privacy"] = value.privacy;
    j["scope"] = to_wire(value.scope);
    j["scope_id"] = value.scope_id;
    j["token"] = value.token;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, SecuritySettings& value) {
    value.certificate = j.at("certificate").get<CertificatePolicy>();
    value.created_at = j.at("created_at").get<std::string>();
    value.email = j.at("email").get<EmailVerificationPolicy>();
    value.id = j.at("id").get<std::string>();
    value.lockout = j.at("lockout").get<LockoutPolicy>();
    value.mfa = j.at("mfa").get<MfaPolicy>();
    value.notification = j.at("notification").get<NotificationPolicy>();
    value.opaque = j.at("opaque").get<OpaquePolicy>();
    value.password = j.at("password").get<PasswordPolicy>();
    value.privacy = j.at("privacy").get<PrivacyPolicy>();
    value.scope = settings_scope_from_wire(j.at("scope").get<std::string>());
    value.scope_id = j.at("scope_id").get<std::string>();
    value.token = j.at("token").get<TokenPolicy>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const ServiceAccountCreatedResponse& value) {
    j = nlohmann::json::object();
    j["client_id"] = value.client_id;
    j["client_secret"] = detail::reveal(value.client_secret);
    j["created_at"] = value.created_at;
    if (value.description) {
        j["description"] = *value.description;
    }
    j["id"] = value.id;
    j["name"] = value.name;
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, ServiceAccountCreatedResponse& value) {
    value.client_id = j.at("client_id").get<std::string>();
    value.client_secret = Sensitive<std::string>(j.at("client_secret").get<std::string>());
    value.created_at = j.at("created_at").get<std::string>();
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.status = user_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const ServiceAccountResponse& value) {
    j = nlohmann::json::object();
    j["client_id"] = value.client_id;
    j["created_at"] = value.created_at;
    if (value.description) {
        j["description"] = *value.description;
    }
    j["id"] = value.id;
    j["name"] = value.name;
    j["status"] = to_wire(value.status);
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, ServiceAccountResponse& value) {
    value.client_id = j.at("client_id").get<std::string>();
    value.created_at = j.at("created_at").get<std::string>();
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    value.id = j.at("id").get<std::string>();
    value.name = j.at("name").get<std::string>();
    value.status = user_status_from_wire(j.at("status").get<std::string>());
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const SetMtlsTrustAnchor& value) {
    j = nlohmann::json::object();
    j["enabled"] = value.enabled;
}

void from_json(const nlohmann::json& j, SetMtlsTrustAnchor& value) {
    value.enabled = j.at("enabled").get<bool>();
}

void to_json(nlohmann::json& j, const SetOrgEmailConfig& value) {
    j = nlohmann::json::object();
    j["enabled"] = value.enabled;
    j["from_email"] = value.from_email;
    j["from_name"] = value.from_name;
    j["provider"] = value.provider;
    if (value.reply_to) {
        j["reply_to"] = *value.reply_to;
    }
}

void from_json(const nlohmann::json& j, SetOrgEmailConfig& value) {
    value.enabled = j.at("enabled").get<bool>();
    value.from_email = j.at("from_email").get<std::string>();
    value.from_name = j.at("from_name").get<std::string>();
    value.provider = j.at("provider").get<ProviderConfig>();
    if (auto it = j.find("reply_to"); it != j.end() && !it->is_null()) {
        value.reply_to = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const SetOrgSettings& value) {
    j = nlohmann::json::object();
    j["access_token_lifetime_secs"] = value.access_token_lifetime_secs;
    j["admin_notifications_enabled"] = value.admin_notifications_enabled;
    j["default_cert_validity_days"] = value.default_cert_validity_days;
    if (value.deletion_grace_period_days) {
        j["deletion_grace_period_days"] = *value.deletion_grace_period_days;
    }
    j["email_verification_grace_period_hours"] = value.email_verification_grace_period_hours;
    j["email_verification_required"] = value.email_verification_required;
    j["hibp_check_enabled"] = value.hibp_check_enabled;
    j["lockout_backoff_multiplier"] = value.lockout_backoff_multiplier;
    j["lockout_duration_secs"] = value.lockout_duration_secs;
    j["max_cert_validity_days"] = value.max_cert_validity_days;
    j["max_failed_login_attempts"] = value.max_failed_login_attempts;
    j["max_lockout_duration_secs"] = value.max_lockout_duration_secs;
    j["mfa_challenge_lifetime_secs"] = value.mfa_challenge_lifetime_secs;
    j["mfa_enforced"] = value.mfa_enforced;
    j["min_length"] = value.min_length;
    if (value.opaque_ksf) {
        j["opaque_ksf"] = *value.opaque_ksf;
    }
    if (value.opaque_mode) {
        j["opaque_mode"] = *value.opaque_mode;
    }
    if (value.opaque_suite) {
        j["opaque_suite"] = *value.opaque_suite;
    }
    j["password_history_count"] = value.password_history_count;
    j["refresh_token_lifetime_secs"] = value.refresh_token_lifetime_secs;
    j["require_digits"] = value.require_digits;
    j["require_lowercase"] = value.require_lowercase;
    j["require_symbols"] = value.require_symbols;
    j["require_uppercase"] = value.require_uppercase;
}

void from_json(const nlohmann::json& j, SetOrgSettings& value) {
    value.access_token_lifetime_secs = j.at("access_token_lifetime_secs").get<std::int64_t>();
    value.admin_notifications_enabled = j.at("admin_notifications_enabled").get<bool>();
    value.default_cert_validity_days = j.at("default_cert_validity_days").get<std::int64_t>();
    if (auto it = j.find("deletion_grace_period_days"); it != j.end() && !it->is_null()) {
        value.deletion_grace_period_days = it->get<std::int64_t>();
    }
    value.email_verification_grace_period_hours = j.at("email_verification_grace_period_hours").get<std::int64_t>();
    value.email_verification_required = j.at("email_verification_required").get<bool>();
    value.hibp_check_enabled = j.at("hibp_check_enabled").get<bool>();
    value.lockout_backoff_multiplier = j.at("lockout_backoff_multiplier").get<double>();
    value.lockout_duration_secs = j.at("lockout_duration_secs").get<std::int64_t>();
    value.max_cert_validity_days = j.at("max_cert_validity_days").get<std::int64_t>();
    value.max_failed_login_attempts = j.at("max_failed_login_attempts").get<std::int64_t>();
    value.max_lockout_duration_secs = j.at("max_lockout_duration_secs").get<std::int64_t>();
    value.mfa_challenge_lifetime_secs = j.at("mfa_challenge_lifetime_secs").get<std::int64_t>();
    value.mfa_enforced = j.at("mfa_enforced").get<bool>();
    value.min_length = j.at("min_length").get<std::int64_t>();
    if (auto it = j.find("opaque_ksf"); it != j.end() && !it->is_null()) {
        value.opaque_ksf = it->get<std::string>();
    }
    if (auto it = j.find("opaque_mode"); it != j.end() && !it->is_null()) {
        value.opaque_mode = it->get<std::string>();
    }
    if (auto it = j.find("opaque_suite"); it != j.end() && !it->is_null()) {
        value.opaque_suite = it->get<std::string>();
    }
    value.password_history_count = j.at("password_history_count").get<std::int64_t>();
    value.refresh_token_lifetime_secs = j.at("refresh_token_lifetime_secs").get<std::int64_t>();
    value.require_digits = j.at("require_digits").get<bool>();
    value.require_lowercase = j.at("require_lowercase").get<bool>();
    value.require_symbols = j.at("require_symbols").get<bool>();
    value.require_uppercase = j.at("require_uppercase").get<bool>();
}

void to_json(nlohmann::json& j, const SignAuditBatchRequest& value) {
    j = nlohmann::json::object();
    j["entry_ids"] = value.entry_ids;
}

void from_json(const nlohmann::json& j, SignAuditBatchRequest& value) {
    value.entry_ids = j.at("entry_ids").get<std::vector<std::string>>();
}

void to_json(nlohmann::json& j, const SignIntermediateCsrRequest& value) {
    j = nlohmann::json::object();
    j["csr_pem"] = value.csr_pem;
    j["parent_ca_id"] = value.parent_ca_id;
    j["validity_days"] = value.validity_days;
}

void from_json(const nlohmann::json& j, SignIntermediateCsrRequest& value) {
    value.csr_pem = j.at("csr_pem").get<std::string>();
    value.parent_ca_id = j.at("parent_ca_id").get<std::string>();
    value.validity_days = j.at("validity_days").get<std::int64_t>();
}

void to_json(nlohmann::json& j, const SignedAuditBatch& value) {
    j = nlohmann::json::object();
    j["batch_id"] = value.batch_id;
    j["entry_ids"] = value.entry_ids;
    j["signature_armored"] = value.signature_armored;
    j["signed_at"] = value.signed_at;
    j["signing_key_id"] = value.signing_key_id;
    j["tenant_id"] = value.tenant_id;
}

void from_json(const nlohmann::json& j, SignedAuditBatch& value) {
    value.batch_id = j.at("batch_id").get<std::string>();
    value.entry_ids = j.at("entry_ids").get<std::vector<std::string>>();
    value.signature_armored = j.at("signature_armored").get<std::string>();
    value.signed_at = j.at("signed_at").get<std::string>();
    value.signing_key_id = j.at("signing_key_id").get<std::string>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
}

void to_json(nlohmann::json& j, const SmtpConfig& value) {
    j = nlohmann::json::object();
    j["host"] = value.host;
    j["port"] = value.port;
    j["starttls"] = value.starttls;
    j["username"] = value.username;
}

void from_json(const nlohmann::json& j, SmtpConfig& value) {
    value.host = j.at("host").get<std::string>();
    value.port = j.at("port").get<std::int64_t>();
    value.starttls = j.at("starttls").get<bool>();
    value.username = j.at("username").get<std::string>();
}

void to_json(nlohmann::json& j, const Tenant& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["id"] = value.id;
    { auto parsed = nlohmann::json::parse(value.metadata, nullptr, false); if (!parsed.is_discarded()) j["metadata"] = parsed; }
    j["name"] = value.name;
    j["organization_id"] = value.organization_id;
    j["slug"] = value.slug;
    j["status"] = to_wire(value.status);
    j["updated_at"] = value.updated_at;
}

void from_json(const nlohmann::json& j, Tenant& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.id = j.at("id").get<std::string>();
    value.metadata = j.at("metadata").dump();
    value.name = j.at("name").get<std::string>();
    value.organization_id = j.at("organization_id").get<std::string>();
    value.slug = j.at("slug").get<std::string>();
    value.status = tenant_status_from_wire(j.at("status").get<std::string>());
    value.updated_at = j.at("updated_at").get<std::string>();
}

void to_json(nlohmann::json& j, const TenantSettingsOverride& value) {
    j = nlohmann::json::object();
    if (value.access_token_lifetime_secs) {
        j["access_token_lifetime_secs"] = *value.access_token_lifetime_secs;
    }
    if (value.admin_notifications_enabled) {
        j["admin_notifications_enabled"] = *value.admin_notifications_enabled;
    }
    if (value.default_cert_validity_days) {
        j["default_cert_validity_days"] = *value.default_cert_validity_days;
    }
    if (value.deletion_grace_period_days) {
        j["deletion_grace_period_days"] = *value.deletion_grace_period_days;
    }
    if (value.email_verification_grace_period_hours) {
        j["email_verification_grace_period_hours"] = *value.email_verification_grace_period_hours;
    }
    if (value.email_verification_required) {
        j["email_verification_required"] = *value.email_verification_required;
    }
    if (value.hibp_check_enabled) {
        j["hibp_check_enabled"] = *value.hibp_check_enabled;
    }
    if (value.lockout_backoff_multiplier) {
        j["lockout_backoff_multiplier"] = *value.lockout_backoff_multiplier;
    }
    if (value.lockout_duration_secs) {
        j["lockout_duration_secs"] = *value.lockout_duration_secs;
    }
    if (value.max_cert_validity_days) {
        j["max_cert_validity_days"] = *value.max_cert_validity_days;
    }
    if (value.max_failed_login_attempts) {
        j["max_failed_login_attempts"] = *value.max_failed_login_attempts;
    }
    if (value.max_lockout_duration_secs) {
        j["max_lockout_duration_secs"] = *value.max_lockout_duration_secs;
    }
    if (value.mfa_challenge_lifetime_secs) {
        j["mfa_challenge_lifetime_secs"] = *value.mfa_challenge_lifetime_secs;
    }
    if (value.mfa_enforced) {
        j["mfa_enforced"] = *value.mfa_enforced;
    }
    if (value.min_length) {
        j["min_length"] = *value.min_length;
    }
    if (value.opaque_ksf) {
        j["opaque_ksf"] = *value.opaque_ksf;
    }
    if (value.opaque_mode) {
        j["opaque_mode"] = *value.opaque_mode;
    }
    if (value.opaque_suite) {
        j["opaque_suite"] = *value.opaque_suite;
    }
    if (value.password_history_count) {
        j["password_history_count"] = *value.password_history_count;
    }
    if (value.refresh_token_lifetime_secs) {
        j["refresh_token_lifetime_secs"] = *value.refresh_token_lifetime_secs;
    }
    if (value.require_digits) {
        j["require_digits"] = *value.require_digits;
    }
    if (value.require_lowercase) {
        j["require_lowercase"] = *value.require_lowercase;
    }
    if (value.require_symbols) {
        j["require_symbols"] = *value.require_symbols;
    }
    if (value.require_uppercase) {
        j["require_uppercase"] = *value.require_uppercase;
    }
}

void from_json(const nlohmann::json& j, TenantSettingsOverride& value) {
    if (auto it = j.find("access_token_lifetime_secs"); it != j.end() && !it->is_null()) {
        value.access_token_lifetime_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("admin_notifications_enabled"); it != j.end() && !it->is_null()) {
        value.admin_notifications_enabled = it->get<bool>();
    }
    if (auto it = j.find("default_cert_validity_days"); it != j.end() && !it->is_null()) {
        value.default_cert_validity_days = it->get<std::int64_t>();
    }
    if (auto it = j.find("deletion_grace_period_days"); it != j.end() && !it->is_null()) {
        value.deletion_grace_period_days = it->get<std::int64_t>();
    }
    if (auto it = j.find("email_verification_grace_period_hours"); it != j.end() && !it->is_null()) {
        value.email_verification_grace_period_hours = it->get<std::int64_t>();
    }
    if (auto it = j.find("email_verification_required"); it != j.end() && !it->is_null()) {
        value.email_verification_required = it->get<bool>();
    }
    if (auto it = j.find("hibp_check_enabled"); it != j.end() && !it->is_null()) {
        value.hibp_check_enabled = it->get<bool>();
    }
    if (auto it = j.find("lockout_backoff_multiplier"); it != j.end() && !it->is_null()) {
        value.lockout_backoff_multiplier = it->get<double>();
    }
    if (auto it = j.find("lockout_duration_secs"); it != j.end() && !it->is_null()) {
        value.lockout_duration_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("max_cert_validity_days"); it != j.end() && !it->is_null()) {
        value.max_cert_validity_days = it->get<std::int64_t>();
    }
    if (auto it = j.find("max_failed_login_attempts"); it != j.end() && !it->is_null()) {
        value.max_failed_login_attempts = it->get<std::int64_t>();
    }
    if (auto it = j.find("max_lockout_duration_secs"); it != j.end() && !it->is_null()) {
        value.max_lockout_duration_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("mfa_challenge_lifetime_secs"); it != j.end() && !it->is_null()) {
        value.mfa_challenge_lifetime_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("mfa_enforced"); it != j.end() && !it->is_null()) {
        value.mfa_enforced = it->get<bool>();
    }
    if (auto it = j.find("min_length"); it != j.end() && !it->is_null()) {
        value.min_length = it->get<std::int64_t>();
    }
    if (auto it = j.find("opaque_ksf"); it != j.end() && !it->is_null()) {
        value.opaque_ksf = it->get<std::string>();
    }
    if (auto it = j.find("opaque_mode"); it != j.end() && !it->is_null()) {
        value.opaque_mode = it->get<std::string>();
    }
    if (auto it = j.find("opaque_suite"); it != j.end() && !it->is_null()) {
        value.opaque_suite = it->get<std::string>();
    }
    if (auto it = j.find("password_history_count"); it != j.end() && !it->is_null()) {
        value.password_history_count = it->get<std::int64_t>();
    }
    if (auto it = j.find("refresh_token_lifetime_secs"); it != j.end() && !it->is_null()) {
        value.refresh_token_lifetime_secs = it->get<std::int64_t>();
    }
    if (auto it = j.find("require_digits"); it != j.end() && !it->is_null()) {
        value.require_digits = it->get<bool>();
    }
    if (auto it = j.find("require_lowercase"); it != j.end() && !it->is_null()) {
        value.require_lowercase = it->get<bool>();
    }
    if (auto it = j.find("require_symbols"); it != j.end() && !it->is_null()) {
        value.require_symbols = it->get<bool>();
    }
    if (auto it = j.find("require_uppercase"); it != j.end() && !it->is_null()) {
        value.require_uppercase = it->get<bool>();
    }
}

void to_json(nlohmann::json& j, const UpdateFederationConfigRequest& value) {
    j = nlohmann::json::object();
    if (value.allowed_algorithms) {
        j["allowed_algorithms"] = *value.allowed_algorithms;
    }
    if (value.attribute_map) {
        auto parsed = nlohmann::json::parse(*value.attribute_map, nullptr, false);
        if (!parsed.is_discarded()) j["attribute_map"] = parsed;
    }
    if (value.client_id) {
        j["client_id"] = *value.client_id;
    }
    if (value.client_secret) {
        j["client_secret"] = detail::reveal(*value.client_secret);
    }
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    if (value.idp_signing_cert_pem) {
        j["idp_signing_cert_pem"] = *value.idp_signing_cert_pem;
    }
    if (value.metadata_url) {
        j["metadata_url"] = *value.metadata_url;
    }
    if (value.provider) {
        j["provider"] = *value.provider;
    }
    if (value.token_exchange) {
        j["token_exchange"] = *value.token_exchange;
    }
}

void from_json(const nlohmann::json& j, UpdateFederationConfigRequest& value) {
    if (auto it = j.find("allowed_algorithms"); it != j.end() && !it->is_null()) {
        value.allowed_algorithms = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("attribute_map"); it != j.end() && !it->is_null()) {
        value.attribute_map = it->dump();
    }
    if (auto it = j.find("client_id"); it != j.end() && !it->is_null()) {
        value.client_id = it->get<std::string>();
    }
    if (auto it = j.find("client_secret"); it != j.end() && !it->is_null()) {
        value.client_secret = Sensitive<std::string>(it->get<std::string>());
    }
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    if (auto it = j.find("idp_signing_cert_pem"); it != j.end() && !it->is_null()) {
        value.idp_signing_cert_pem = it->get<std::string>();
    }
    if (auto it = j.find("metadata_url"); it != j.end() && !it->is_null()) {
        value.metadata_url = it->get<std::string>();
    }
    if (auto it = j.find("provider"); it != j.end() && !it->is_null()) {
        value.provider = it->get<std::string>();
    }
    if (auto it = j.find("token_exchange"); it != j.end() && !it->is_null()) {
        value.token_exchange = it->get<TokenExchangeTrustRequest>();
    }
}

void to_json(nlohmann::json& j, const UpdateGroup& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
}

void from_json(const nlohmann::json& j, UpdateGroup& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdateNotificationRuleRequest& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    if (value.events) {
        j["events"] = *value.events;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.recipient_emails) {
        j["recipient_emails"] = *value.recipient_emails;
    }
}

void from_json(const nlohmann::json& j, UpdateNotificationRuleRequest& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    if (auto it = j.find("events"); it != j.end() && !it->is_null()) {
        value.events = it->get<std::vector<NotificationEventType>>();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("recipient_emails"); it != j.end() && !it->is_null()) {
        value.recipient_emails = it->get<std::vector<std::string>>();
    }
}

void to_json(nlohmann::json& j, const UpdateOAuth2ClientRequest& value) {
    j = nlohmann::json::object();
    if (value.backchannel_logout_uri) {
        j["backchannel_logout_uri"] = *value.backchannel_logout_uri;
    }
    if (value.dpop_bound_access_tokens) {
        j["dpop_bound_access_tokens"] = *value.dpop_bound_access_tokens;
    }
    if (value.dpop_require_nonce) {
        j["dpop_require_nonce"] = *value.dpop_require_nonce;
    }
    if (value.grant_types) {
        j["grant_types"] = *value.grant_types;
    }
    if (value.jwks) {
        j["jwks"] = *value.jwks;
    }
    if (value.jwks_uri) {
        j["jwks_uri"] = *value.jwks_uri;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.post_logout_redirect_uris) {
        j["post_logout_redirect_uris"] = *value.post_logout_redirect_uris;
    }
    if (value.profile) {
        j["profile"] = to_wire(*value.profile);
    }
    if (value.redirect_uris) {
        j["redirect_uris"] = *value.redirect_uris;
    }
    if (value.require_par) {
        j["require_par"] = *value.require_par;
    }
    if (value.scopes) {
        j["scopes"] = *value.scopes;
    }
    if (value.self_signed_tls_client_auth_thumbprints) {
        j["self_signed_tls_client_auth_thumbprints"] = *value.self_signed_tls_client_auth_thumbprints;
    }
    if (value.tls_client_auth_san_dns) {
        j["tls_client_auth_san_dns"] = *value.tls_client_auth_san_dns;
    }
    if (value.tls_client_auth_san_uri) {
        j["tls_client_auth_san_uri"] = *value.tls_client_auth_san_uri;
    }
    if (value.tls_client_auth_subject_dn) {
        j["tls_client_auth_subject_dn"] = *value.tls_client_auth_subject_dn;
    }
    if (value.tls_client_certificate_bound_access_tokens) {
        j["tls_client_certificate_bound_access_tokens"] = *value.tls_client_certificate_bound_access_tokens;
    }
    if (value.token_endpoint_auth_method) {
        j["token_endpoint_auth_method"] = to_wire(*value.token_endpoint_auth_method);
    }
}

void from_json(const nlohmann::json& j, UpdateOAuth2ClientRequest& value) {
    if (auto it = j.find("backchannel_logout_uri"); it != j.end() && !it->is_null()) {
        value.backchannel_logout_uri = it->get<std::string>();
    }
    if (auto it = j.find("dpop_bound_access_tokens"); it != j.end() && !it->is_null()) {
        value.dpop_bound_access_tokens = it->get<bool>();
    }
    if (auto it = j.find("dpop_require_nonce"); it != j.end() && !it->is_null()) {
        value.dpop_require_nonce = it->get<bool>();
    }
    if (auto it = j.find("grant_types"); it != j.end() && !it->is_null()) {
        value.grant_types = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("jwks"); it != j.end() && !it->is_null()) {
        value.jwks = it->get<std::string>();
    }
    if (auto it = j.find("jwks_uri"); it != j.end() && !it->is_null()) {
        value.jwks_uri = it->get<std::string>();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("post_logout_redirect_uris"); it != j.end() && !it->is_null()) {
        value.post_logout_redirect_uris = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("profile"); it != j.end() && !it->is_null()) {
        value.profile = client_profile_from_wire(it->get<std::string>());
    }
    if (auto it = j.find("redirect_uris"); it != j.end() && !it->is_null()) {
        value.redirect_uris = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("require_par"); it != j.end() && !it->is_null()) {
        value.require_par = it->get<bool>();
    }
    if (auto it = j.find("scopes"); it != j.end() && !it->is_null()) {
        value.scopes = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("self_signed_tls_client_auth_thumbprints"); it != j.end() && !it->is_null()) {
        value.self_signed_tls_client_auth_thumbprints = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("tls_client_auth_san_dns"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_san_dns = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_auth_san_uri"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_san_uri = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_auth_subject_dn"); it != j.end() && !it->is_null()) {
        value.tls_client_auth_subject_dn = it->get<std::string>();
    }
    if (auto it = j.find("tls_client_certificate_bound_access_tokens"); it != j.end() && !it->is_null()) {
        value.tls_client_certificate_bound_access_tokens = it->get<bool>();
    }
    if (auto it = j.find("token_endpoint_auth_method"); it != j.end() && !it->is_null()) {
        value.token_endpoint_auth_method = client_auth_method_from_wire(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const UpdateOrganizationRequest& value) {
    j = nlohmann::json::object();
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.slug) {
        j["slug"] = *value.slug;
    }
}

void from_json(const nlohmann::json& j, UpdateOrganizationRequest& value) {
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("slug"); it != j.end() && !it->is_null()) {
        value.slug = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdatePermissionRequest& value) {
    j = nlohmann::json::object();
    if (value.action) {
        j["action"] = *value.action;
    }
    if (value.description) {
        j["description"] = *value.description;
    }
}

void from_json(const nlohmann::json& j, UpdatePermissionRequest& value) {
    if (auto it = j.find("action"); it != j.end() && !it->is_null()) {
        value.action = it->get<std::string>();
    }
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdateReactorRequest& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    if (value.events) {
        j["events"] = *value.events;
    }
    if (value.failure_policy) {
        j["failure_policy"] = to_wire(*value.failure_policy);
    }
    if (value.mode) {
        j["mode"] = to_wire(*value.mode);
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.priority) {
        j["priority"] = *value.priority;
    }
    if (value.timeout_ms) {
        j["timeout_ms"] = *value.timeout_ms;
    }
}

void from_json(const nlohmann::json& j, UpdateReactorRequest& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    if (auto it = j.find("events"); it != j.end() && !it->is_null()) {
        value.events = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("failure_policy"); it != j.end() && !it->is_null()) {
        value.failure_policy = failure_policy_from_wire(it->get<std::string>());
    }
    if (auto it = j.find("mode"); it != j.end() && !it->is_null()) {
        value.mode = reactor_mode_from_wire(it->get<std::string>());
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("priority"); it != j.end() && !it->is_null()) {
        value.priority = it->get<std::int64_t>();
    }
    if (auto it = j.find("timeout_ms"); it != j.end() && !it->is_null()) {
        value.timeout_ms = it->get<std::int64_t>();
    }
}

void to_json(nlohmann::json& j, const UpdateResourceRequest& value) {
    j = nlohmann::json::object();
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.parent_id) {
        j["parent_id"] = *value.parent_id;
    }
    if (value.resource_type) {
        j["resource_type"] = *value.resource_type;
    }
}

void from_json(const nlohmann::json& j, UpdateResourceRequest& value) {
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("parent_id"); it != j.end() && !it->is_null()) {
        value.parent_id = it->get<std::string>();
    }
    if (auto it = j.find("resource_type"); it != j.end() && !it->is_null()) {
        value.resource_type = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdateRole& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.is_global) {
        j["is_global"] = *value.is_global;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
}

void from_json(const nlohmann::json& j, UpdateRole& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("is_global"); it != j.end() && !it->is_null()) {
        value.is_global = it->get<bool>();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdateScopeRequest& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
}

void from_json(const nlohmann::json& j, UpdateScopeRequest& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdateServiceAccount& value) {
    j = nlohmann::json::object();
    if (value.description) {
        j["description"] = *value.description;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.status) {
        j["status"] = to_wire(*value.status);
    }
}

void from_json(const nlohmann::json& j, UpdateServiceAccount& value) {
    if (auto it = j.find("description"); it != j.end() && !it->is_null()) {
        value.description = it->get<std::string>();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("status"); it != j.end() && !it->is_null()) {
        value.status = user_status_from_wire(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const UpdateTenant& value) {
    j = nlohmann::json::object();
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    if (value.name) {
        j["name"] = *value.name;
    }
    if (value.slug) {
        j["slug"] = *value.slug;
    }
    if (value.status) {
        j["status"] = to_wire(*value.status);
    }
}

void from_json(const nlohmann::json& j, UpdateTenant& value) {
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    if (auto it = j.find("name"); it != j.end() && !it->is_null()) {
        value.name = it->get<std::string>();
    }
    if (auto it = j.find("slug"); it != j.end() && !it->is_null()) {
        value.slug = it->get<std::string>();
    }
    if (auto it = j.find("status"); it != j.end() && !it->is_null()) {
        value.status = tenant_status_from_wire(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const UpdateUserRequest& value) {
    j = nlohmann::json::object();
    if (value.email) {
        j["email"] = *value.email;
    }
    if (value.metadata) {
        auto parsed = nlohmann::json::parse(*value.metadata, nullptr, false);
        if (!parsed.is_discarded()) j["metadata"] = parsed;
    }
    if (value.status) {
        j["status"] = to_wire(*value.status);
    }
    if (value.username) {
        j["username"] = *value.username;
    }
}

void from_json(const nlohmann::json& j, UpdateUserRequest& value) {
    if (auto it = j.find("email"); it != j.end() && !it->is_null()) {
        value.email = it->get<std::string>();
    }
    if (auto it = j.find("metadata"); it != j.end() && !it->is_null()) {
        value.metadata = it->dump();
    }
    if (auto it = j.find("status"); it != j.end() && !it->is_null()) {
        value.status = user_status_from_wire(it->get<std::string>());
    }
    if (auto it = j.find("username"); it != j.end() && !it->is_null()) {
        value.username = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const UpdateWebhookRequest& value) {
    j = nlohmann::json::object();
    if (value.enabled) {
        j["enabled"] = *value.enabled;
    }
    if (value.events) {
        j["events"] = *value.events;
    }
    if (value.retry_policy) {
        j["retry_policy"] = *value.retry_policy;
    }
    if (value.secret) {
        j["secret"] = detail::reveal(*value.secret);
    }
    if (value.url) {
        j["url"] = *value.url;
    }
}

void from_json(const nlohmann::json& j, UpdateWebhookRequest& value) {
    if (auto it = j.find("enabled"); it != j.end() && !it->is_null()) {
        value.enabled = it->get<bool>();
    }
    if (auto it = j.find("events"); it != j.end() && !it->is_null()) {
        value.events = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("retry_policy"); it != j.end() && !it->is_null()) {
        value.retry_policy = it->get<RetryPolicy>();
    }
    if (auto it = j.find("secret"); it != j.end() && !it->is_null()) {
        value.secret = Sensitive<std::string>(it->get<std::string>());
    }
    if (auto it = j.find("url"); it != j.end() && !it->is_null()) {
        value.url = it->get<std::string>();
    }
}

void to_json(nlohmann::json& j, const WebauthnAttestationPolicy& value) {
    j = nlohmann::json::object();
    if (value.allowed_aaguids) {
        j["allowed_aaguids"] = *value.allowed_aaguids;
    }
    j["block_revoked_status"] = value.block_revoked_status;
    if (value.blocked_aaguids) {
        j["blocked_aaguids"] = *value.blocked_aaguids;
    }
    if (value.min_certification) {
        j["min_certification"] = to_wire(*value.min_certification);
    }
    j["mode"] = to_wire(value.mode);
    j["require_fido_certified"] = value.require_fido_certified;
    if (value.unknown_aaguid) {
        j["unknown_aaguid"] = to_wire(*value.unknown_aaguid);
    }
}

void from_json(const nlohmann::json& j, WebauthnAttestationPolicy& value) {
    if (auto it = j.find("allowed_aaguids"); it != j.end() && !it->is_null()) {
        value.allowed_aaguids = it->get<std::vector<std::string>>();
    }
    value.block_revoked_status = j.at("block_revoked_status").get<bool>();
    if (auto it = j.find("blocked_aaguids"); it != j.end() && !it->is_null()) {
        value.blocked_aaguids = it->get<std::vector<std::string>>();
    }
    if (auto it = j.find("min_certification"); it != j.end() && !it->is_null()) {
        value.min_certification = certification_level_from_wire(it->get<std::string>());
    }
    value.mode = attestation_mode_from_wire(j.at("mode").get<std::string>());
    value.require_fido_certified = j.at("require_fido_certified").get<bool>();
    if (auto it = j.find("unknown_aaguid"); it != j.end() && !it->is_null()) {
        value.unknown_aaguid = unknown_aaguid_action_from_wire(it->get<std::string>());
    }
}

void to_json(nlohmann::json& j, const WebhookResponse& value) {
    j = nlohmann::json::object();
    j["created_at"] = value.created_at;
    j["enabled"] = value.enabled;
    j["events"] = value.events;
    j["id"] = value.id;
    j["retry_policy"] = value.retry_policy;
    j["tenant_id"] = value.tenant_id;
    j["updated_at"] = value.updated_at;
    j["url"] = value.url;
}

void from_json(const nlohmann::json& j, WebhookResponse& value) {
    value.created_at = j.at("created_at").get<std::string>();
    value.enabled = j.at("enabled").get<bool>();
    value.events = j.at("events").get<std::vector<std::string>>();
    value.id = j.at("id").get<std::string>();
    value.retry_policy = j.at("retry_policy").get<RetryPolicy>();
    value.tenant_id = j.at("tenant_id").get<std::string>();
    value.updated_at = j.at("updated_at").get<std::string>();
    value.url = j.at("url").get<std::string>();
}

}  // namespace axiam::management
