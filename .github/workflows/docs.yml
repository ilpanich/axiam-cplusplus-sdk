name: docs

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: docs-gh-pages
  cancel-in-progress: true

jobs:
  doxygen:
    name: build & publish Doxygen HTML to gh-pages
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4.4.0
      - name: Install Doxygen
        run: |
          sudo apt-get update
          sudo apt-get install -y doxygen graphviz
      - name: Generate docs
        run: |
          set -euo pipefail
          # PROJECT_NUMBER is printed in the header of every generated page.
          # Read it from vcpkg.json, which the release bump does touch, rather
          # than from the Doxyfile literal, which it does not: that literal sat
          # at 1.0.0-alpha8 and was published as the version of every beta.
          # (axiam.hpp's kVersion is only "1.0.0" — it carries no pre-release
          # qualifier, so it cannot serve as the documented version.) Doxygen
          # keeps the LAST assignment of a tag, so appending one overrides the
          # file's own.
          version=$(jq -er .version vcpkg.json)
          echo "Documenting $version"
          { cat Doxyfile; echo "PROJECT_NUMBER = $version"; } | doxygen -
      - name: Publish to gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          DIR=docs/html
          touch "$DIR/.nojekyll"
          cd "$DIR"
          git init -q
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -q -m "docs: publish from ${GITHUB_SHA}"
          git push -f "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:gh-pages
