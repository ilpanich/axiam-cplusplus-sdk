// Generated by scripts/gen_management.py from management-registry.json and openapi.json.
// DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.


#include <stdexcept>
#include <string>

#include "assert.hpp"
#include "axiam/axiam.hpp"
#include "axiam/management.hpp"

// Internal, not installed -- see the generator's emit_models_test() docstring.
#include "management_json.hpp"

#include "management_test_util.hpp"

namespace {

// Only the management namespace is opened. `using namespace axiam;` alongside it makes names
// the two share -- OpaqueEnrollment is one -- ambiguous, and the resulting diagnostic ("parse
// error in template argument list") points at the use site rather than the directive.
using namespace axiam::management;

AXIAM_TEST("management model AddMemberRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<AddMemberRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<AddMemberRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ApiProviderConfig round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"api_url": "example"})json");

    const auto value = wire.get<ApiProviderConfig>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ApiProviderConfig>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model AssignRoleToGroupRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"group_id": "11111111-1111-4111-8111-111111111111", "resource_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<AssignRoleToGroupRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<AssignRoleToGroupRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model AssignRoleToUserRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"resource_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<AssignRoleToUserRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<AssignRoleToUserRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model AuditLogEntry round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"action": "example", "actor_id": "11111111-1111-4111-8111-111111111111", "actor_type": "User", "id": "11111111-1111-4111-8111-111111111111", "ip_address": "example", "metadata": {}, "outcome": "Success", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "timestamp": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<AuditLogEntry>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<AuditLogEntry>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model BindCertificate round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"certificate_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<BindCertificate>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<BindCertificate>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CaCertificate round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "key_custody": "example", "key_locator": "example", "mtls_trust_anchor": true, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "organization_id": "11111111-1111-4111-8111-111111111111", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<CaCertificate>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CaCertificate>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Certificate round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"cert_type": "User", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<Certificate>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Certificate>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CertificatePolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"default_cert_validity_days": 1, "max_cert_validity_days": 1})json");

    const auto value = wire.get<CertificatePolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CertificatePolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ComplianceReportEntry round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"aaguid": "11111111-1111-4111-8111-111111111111", "authenticator_name": "example", "compliant": true, "credential_id": "11111111-1111-4111-8111-111111111111", "name": "example", "reason": "example", "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<ComplianceReportEntry>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ComplianceReportEntry>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateCaCertificateRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"intermediate_subject": "example", "intermediate_validity_days": 1, "issue_from_root": true, "key_algorithm": "Rsa4096", "subject": "example", "validity_days": 1})json");

    const auto value = wire.get<CreateCaCertificateRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateCaCertificateRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateCertificateRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"cert_type": "User", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "subject": "example", "validity_days": 1})json");

    const auto value = wire.get<CreateCertificateRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateCertificateRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model TokenExchangeTrustRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"})json");

    const auto value = wire.get<TokenExchangeTrustRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<TokenExchangeTrustRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateFederationConfigRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"allowed_algorithms": ["example"], "attribute_map": {}, "client_id": "example", "client_secret": "example", "idp_signing_cert_pem": "example", "metadata_url": "example", "protocol": "example", "provider": "example", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}})json");

    const auto value = wire.get<CreateFederationConfigRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateFederationConfigRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateGroupRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "metadata": {}, "name": "example"})json");

    const auto value = wire.get<CreateGroupRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateGroupRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateIntermediateCaRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"key_algorithm": "Rsa4096", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "subject": "example", "validity_days": 1})json");

    const auto value = wire.get<CreateIntermediateCaRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateIntermediateCaRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateNotificationRuleRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "events": ["login_failure"], "name": "example", "recipient_emails": ["example"]})json");

    const auto value = wire.get<CreateNotificationRuleRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateNotificationRuleRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateOAuth2ClientRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"backchannel_logout_uri": "example", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "jwks": "example", "jwks_uri": "example", "name": "example", "post_logout_redirect_uris": ["example"], "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post"})json");

    const auto value = wire.get<CreateOAuth2ClientRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateOAuth2ClientRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreatePermissionRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"action": "example", "description": "example"})json");

    const auto value = wire.get<CreatePermissionRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreatePermissionRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreatePgpKeyRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"algorithm": "Rsa4096", "email": "example", "name": "example", "purpose": "AuditSigning"})json");

    const auto value = wire.get<CreatePgpKeyRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreatePgpKeyRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateReactorRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "mode": "intercept", "name": "example", "priority": 1, "timeout_ms": 1})json");

    const auto value = wire.get<CreateReactorRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateReactorRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateResourceRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example"})json");

    const auto value = wire.get<CreateResourceRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateResourceRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateRoleRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "is_global": true, "name": "example"})json");

    const auto value = wire.get<CreateRoleRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateRoleRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateScimTokenRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"expires_in_days": 1, "name": "example", "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<CreateScimTokenRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateScimTokenRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateScimTokenResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "created_by": "11111111-1111-4111-8111-111111111111", "expires_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "last_used_at": "2026-08-26T00:00:00Z", "name": "example", "provisioning_token": "example", "revoked_at": "2026-08-26T00:00:00Z", "status": "active", "tenant_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<CreateScimTokenResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateScimTokenResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateScopeRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "name": "example"})json");

    const auto value = wire.get<CreateScopeRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateScopeRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateServiceAccountRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "name": "example"})json");

    const auto value = wire.get<CreateServiceAccountRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateServiceAccountRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateTenantRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"metadata": {}, "name": "example", "slug": "example"})json");

    const auto value = wire.get<CreateTenantRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateTenantRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OpaqueEnrollment round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"opaque_session": "example", "registration_record": "example"})json");

    const auto value = wire.get<OpaqueEnrollment>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OpaqueEnrollment>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateUserRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"email": "example", "metadata": {}, "opaque": {"opaque_session": "example", "registration_record": "example"}, "password": "example", "username": "example"})json");

    const auto value = wire.get<CreateUserRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateUserRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model RetryPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1})json");

    const auto value = wire.get<RetryPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<RetryPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model CreateWebhookRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"events": ["example"], "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "secret": "example", "url": "example"})json");

    const auto value = wire.get<CreateWebhookRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<CreateWebhookRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ProviderConfig round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"host": "example", "kind": "smtp", "port": 1, "starttls": true, "username": "example"})json");

    const auto value = wire.get<ProviderConfig>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ProviderConfig>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model EmailConfig round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "enabled": true, "from_email": "example", "from_name": "example", "id": "11111111-1111-4111-8111-111111111111", "provider": {"host": "example", "kind": "smtp", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example", "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<EmailConfig>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<EmailConfig>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model EmailConfigOverride round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"enabled": true, "from_email": "example", "from_name": "example", "provider": {"host": "example", "kind": "smtp", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example"})json");

    const auto value = wire.get<EmailConfigOverride>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<EmailConfigOverride>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model EmailTestResult round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"message_id": "example", "provider": "example", "to": "example"})json");

    const auto value = wire.get<EmailTestResult>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<EmailTestResult>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model EmailVerificationPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"email_verification_grace_period_hours": 1, "email_verification_required": true})json");

    const auto value = wire.get<EmailVerificationPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<EmailVerificationPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model EncryptRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"data_base64": "example"})json");

    const auto value = wire.get<EncryptRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<EncryptRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model EncryptedExport round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"ciphertext_armored": "example", "recipient_key_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<EncryptedExport>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<EncryptedExport>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model TokenExchangeTrustResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"})json");

    const auto value = wire.get<TokenExchangeTrustResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<TokenExchangeTrustResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model FederationConfigResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"attribute_map": {}, "client_id": "example", "created_at": "2026-08-26T00:00:00Z", "enabled": true, "id": "11111111-1111-4111-8111-111111111111", "metadata_url": "example", "protocol": "example", "provider": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}, "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<FederationConfigResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<FederationConfigResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model FederationLinkResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "external_email": "example", "external_subject": "example", "federation_config_id": "11111111-1111-4111-8111-111111111111", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<FederationLinkResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<FederationLinkResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model GeneratedCaCertificate round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "key_custody": "example", "key_locator": "example", "mtls_trust_anchor": true, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "organization_id": "11111111-1111-4111-8111-111111111111", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "private_key_pem": "example", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<GeneratedCaCertificate>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<GeneratedCaCertificate>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model GeneratedCertificate round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"cert_type": "User", "chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "private_key_pem": "example", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<GeneratedCertificate>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<GeneratedCertificate>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model GeneratedPgpKey round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"algorithm": "Rsa4096", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "private_key_armored": "example", "public_key_armored": "example", "purpose": "AuditSigning", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<GeneratedPgpKey>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<GeneratedPgpKey>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model GrantPermissionRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"effect": "allow", "permission_id": "11111111-1111-4111-8111-111111111111", "scope_ids": ["11111111-1111-4111-8111-111111111111"]})json");

    const auto value = wire.get<GrantPermissionRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<GrantPermissionRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model GrantedScope round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<GrantedScope>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<GrantedScope>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Group round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Group>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Group>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model HealthResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"status": "example"})json");

    const auto value = wire.get<HealthResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<HealthResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ImportCaCertificateRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"private_key_pem": "example", "public_cert_pem": "example"})json");

    const auto value = wire.get<ImportCaCertificateRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ImportCaCertificateRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model LockoutPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1})json");

    const auto value = wire.get<LockoutPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<LockoutPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model MdsRefreshOutcome round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"entry_count": 1, "no": 1, "outcome": "initial"})json");

    const auto value = wire.get<MdsRefreshOutcome>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<MdsRefreshOutcome>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model MdsStatusResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"entry_count": 1, "last_refreshed_at": "2026-08-26T00:00:00Z", "next_update": "example", "no": 1, "stale": true})json");

    const auto value = wire.get<MdsStatusResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<MdsStatusResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model MfaMethodResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "example", "last_used_at": "example", "method_id": "example", "method_type": "Totp", "name": "example"})json");

    const auto value = wire.get<MfaMethodResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<MfaMethodResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model MfaPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"mfa_challenge_lifetime_secs": 1, "mfa_enforced": true})json");

    const auto value = wire.get<MfaPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<MfaPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model MigrateCustodyResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"ca_certificate_id": "11111111-1111-4111-8111-111111111111", "key_custody": "example", "key_locator": "example", "previous_custody": "example"})json");

    const auto value = wire.get<MigrateCustodyResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<MigrateCustodyResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model MtlsTrustAnchorResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"ca_certificate_id": "11111111-1111-4111-8111-111111111111", "message": "example", "mtls_trust_anchor": true, "restart_required": true, "trusted_anchors": 1})json");

    const auto value = wire.get<MtlsTrustAnchorResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<MtlsTrustAnchorResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model NotificationPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"admin_notifications_enabled": true})json");

    const auto value = wire.get<NotificationPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<NotificationPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model NotificationRuleResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["login_failure"], "id": "11111111-1111-4111-8111-111111111111", "name": "example", "recipient_emails": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<NotificationRuleResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<NotificationRuleResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OAuth2ClientCreatedResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"client_id": "example", "client_secret": "example", "created_at": "2026-08-26T00:00:00Z", "grant_types": ["example"], "id": "11111111-1111-4111-8111-111111111111", "name": "example", "redirect_uris": ["example"], "scopes": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<OAuth2ClientCreatedResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OAuth2ClientCreatedResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OAuth2ClientResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "id": "11111111-1111-4111-8111-111111111111", "jwks": "example", "jwks_uri": "example", "name": "example", "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<OAuth2ClientResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OAuth2ClientResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OidcAuthorizeRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"config_id": "11111111-1111-4111-8111-111111111111", "nonce": "example", "redirect_uri": "example", "state": "example"})json");

    const auto value = wire.get<OidcAuthorizeRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OidcAuthorizeRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OidcAuthorizeResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"url": "example"})json");

    const auto value = wire.get<OidcAuthorizeResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OidcAuthorizeResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OidcCallbackRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"code": "example", "config_id": "11111111-1111-4111-8111-111111111111", "nonce": "example", "redirect_uri": "example", "state": "example"})json");

    const auto value = wire.get<OidcCallbackRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OidcCallbackRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OidcCallbackResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"federation_link_id": "11111111-1111-4111-8111-111111111111", "newly_provisioned": true, "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<OidcCallbackResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OidcCallbackResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model OpaquePolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example"})json");

    const auto value = wire.get<OpaquePolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<OpaquePolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Organization round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "slug": "example", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Organization>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Organization>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model PasswordPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"hibp_check_enabled": true, "min_length": 1, "password_history_count": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true})json");

    const auto value = wire.get<PasswordPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<PasswordPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Permission round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"action": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Permission>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Permission>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model PgpKey round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"algorithm": "Rsa4096", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "public_key_armored": "example", "purpose": "AuditSigning", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<PgpKey>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<PgpKey>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model PolicyResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"allowed_aaguids": ["11111111-1111-4111-8111-111111111111"], "block_revoked_status": true, "blocked_aaguids": ["11111111-1111-4111-8111-111111111111"], "effective_unknown_aaguid": "allow", "min_certification": "L1", "mode": "none", "require_fido_certified": true, "unknown_aaguid": "allow"})json");

    const auto value = wire.get<PolicyResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<PolicyResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model PrivacyPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"deletion_grace_period_days": 1})json");

    const auto value = wire.get<PrivacyPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<PrivacyPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ReactorEventDescriptor round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"default_failure_policy": "fail_closed", "description": "example", "interceptable": true, "mutable": true, "mutable_fields": ["example"], "name": "example"})json");

    const auto value = wire.get<ReactorEventDescriptor>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ReactorEventDescriptor>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ReactorResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "id": "11111111-1111-4111-8111-111111111111", "last_seen_at": "2026-08-26T00:00:00Z", "mode": "intercept", "name": "example", "priority": 1, "recent_timeout_count": 1, "recent_veto_count": 1, "tenant_id": "11111111-1111-4111-8111-111111111111", "timeout_ms": 1, "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<ReactorResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ReactorResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ReadyResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"database": "example", "status": "example"})json");

    const auto value = wire.get<ReadyResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ReadyResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ResolvedPermissionGrant round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"effect": "allow", "permission": {"action": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}, "scope_ids": ["11111111-1111-4111-8111-111111111111"], "scopes": [{"id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111"}]})json");

    const auto value = wire.get<ResolvedPermissionGrant>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ResolvedPermissionGrant>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Resource round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "uma_registered_by": "example", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Resource>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Resource>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Role round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "is_global": true, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Role>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Role>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model RoleAssignment round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"resource_id": "11111111-1111-4111-8111-111111111111", "role": {"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "is_global": true, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}})json");

    const auto value = wire.get<RoleAssignment>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<RoleAssignment>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model RoleGroupAssignment round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"group": {"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}, "resource_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<RoleGroupAssignment>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<RoleGroupAssignment>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UserResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "email": "example", "email_verified": true, "failed_login_attempts": 1, "id": "11111111-1111-4111-8111-111111111111", "is_locked": true, "locked_until": "2026-08-26T00:00:00Z", "metadata": {}, "mfa_enabled": true, "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "username": "example"})json");

    const auto value = wire.get<UserResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UserResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model RoleUserAssignment round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"resource_id": "11111111-1111-4111-8111-111111111111", "user": {"created_at": "2026-08-26T00:00:00Z", "email": "example", "email_verified": true, "failed_login_attempts": 1, "id": "11111111-1111-4111-8111-111111111111", "is_locked": true, "locked_until": "2026-08-26T00:00:00Z", "metadata": {}, "mfa_enabled": true, "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "username": "example"}})json");

    const auto value = wire.get<RoleUserAssignment>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<RoleUserAssignment>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model RotateSecretResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"client_secret": "example"})json");

    const auto value = wire.get<RotateSecretResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<RotateSecretResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ScimTokenResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "created_by": "11111111-1111-4111-8111-111111111111", "expires_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "last_used_at": "2026-08-26T00:00:00Z", "name": "example", "revoked_at": "2026-08-26T00:00:00Z", "status": "active", "tenant_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<ScimTokenResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ScimTokenResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Scope round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Scope>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Scope>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model TokenPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"access_token_lifetime_secs": 1, "refresh_token_lifetime_secs": 1})json");

    const auto value = wire.get<TokenPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<TokenPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SecuritySettings round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"certificate": {"default_cert_validity_days": 1, "max_cert_validity_days": 1}, "created_at": "2026-08-26T00:00:00Z", "email": {"email_verification_grace_period_hours": 1, "email_verification_required": true}, "id": "11111111-1111-4111-8111-111111111111", "lockout": {"lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1}, "mfa": {"mfa_challenge_lifetime_secs": 1, "mfa_enforced": true}, "notification": {"admin_notifications_enabled": true}, "opaque": {"opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example"}, "password": {"hibp_check_enabled": true, "min_length": 1, "password_history_count": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true}, "privacy": {"deletion_grace_period_days": 1}, "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "token": {"access_token_lifetime_secs": 1, "refresh_token_lifetime_secs": 1}, "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<SecuritySettings>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SecuritySettings>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ServiceAccountCreatedResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"client_id": "example", "client_secret": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<ServiceAccountCreatedResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ServiceAccountCreatedResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model ServiceAccountResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<ServiceAccountResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<ServiceAccountResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SetMtlsTrustAnchor round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"enabled": true})json");

    const auto value = wire.get<SetMtlsTrustAnchor>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SetMtlsTrustAnchor>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SetOrgEmailConfig round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"enabled": true, "from_email": "example", "from_name": "example", "provider": {"host": "example", "kind": "smtp", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example"})json");

    const auto value = wire.get<SetOrgEmailConfig>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SetOrgEmailConfig>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SetOrgSettings round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"access_token_lifetime_secs": 1, "admin_notifications_enabled": true, "default_cert_validity_days": 1, "deletion_grace_period_days": 1, "email_verification_grace_period_hours": 1, "email_verification_required": true, "hibp_check_enabled": true, "lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_cert_validity_days": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1, "mfa_challenge_lifetime_secs": 1, "mfa_enforced": true, "min_length": 1, "opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example", "password_history_count": 1, "refresh_token_lifetime_secs": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true})json");

    const auto value = wire.get<SetOrgSettings>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SetOrgSettings>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SignAuditBatchRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"entry_ids": ["11111111-1111-4111-8111-111111111111"]})json");

    const auto value = wire.get<SignAuditBatchRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SignAuditBatchRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SignIntermediateCsrRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"csr_pem": "example", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "validity_days": 1})json");

    const auto value = wire.get<SignIntermediateCsrRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SignIntermediateCsrRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SignedAuditBatch round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"batch_id": "11111111-1111-4111-8111-111111111111", "entry_ids": ["11111111-1111-4111-8111-111111111111"], "signature_armored": "example", "signed_at": "2026-08-26T00:00:00Z", "signing_key_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111"})json");

    const auto value = wire.get<SignedAuditBatch>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SignedAuditBatch>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model SmtpConfig round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"host": "example", "port": 1, "starttls": true, "username": "example"})json");

    const auto value = wire.get<SmtpConfig>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<SmtpConfig>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model Tenant round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "kind": "standard", "metadata": {}, "name": "example", "organization_id": "11111111-1111-4111-8111-111111111111", "slug": "example", "status": "Active", "updated_at": "2026-08-26T00:00:00Z"})json");

    const auto value = wire.get<Tenant>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<Tenant>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model TenantSettingsOverride round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"access_token_lifetime_secs": 1, "admin_notifications_enabled": true, "default_cert_validity_days": 1, "deletion_grace_period_days": 1, "email_verification_grace_period_hours": 1, "email_verification_required": true, "hibp_check_enabled": true, "lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_cert_validity_days": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1, "mfa_challenge_lifetime_secs": 1, "mfa_enforced": true, "min_length": 1, "opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example", "password_history_count": 1, "refresh_token_lifetime_secs": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true})json");

    const auto value = wire.get<TenantSettingsOverride>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<TenantSettingsOverride>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateFederationConfigRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"allowed_algorithms": ["example"], "attribute_map": {}, "client_id": "example", "client_secret": "example", "enabled": true, "idp_signing_cert_pem": "example", "metadata_url": "example", "provider": "example", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}})json");

    const auto value = wire.get<UpdateFederationConfigRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateFederationConfigRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateGroup round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "metadata": {}, "name": "example"})json");

    const auto value = wire.get<UpdateGroup>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateGroup>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateNotificationRuleRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "enabled": true, "events": ["login_failure"], "name": "example", "recipient_emails": ["example"]})json");

    const auto value = wire.get<UpdateNotificationRuleRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateNotificationRuleRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateOAuth2ClientRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"backchannel_logout_uri": "example", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "jwks": "example", "jwks_uri": "example", "name": "example", "post_logout_redirect_uris": ["example"], "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post"})json");

    const auto value = wire.get<UpdateOAuth2ClientRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateOAuth2ClientRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateOrganizationRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"metadata": {}, "name": "example", "slug": "example"})json");

    const auto value = wire.get<UpdateOrganizationRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateOrganizationRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdatePermissionRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"action": "example", "description": "example"})json");

    const auto value = wire.get<UpdatePermissionRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdatePermissionRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateReactorRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "mode": "intercept", "name": "example", "priority": 1, "timeout_ms": 1})json");

    const auto value = wire.get<UpdateReactorRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateReactorRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateResourceRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example"})json");

    const auto value = wire.get<UpdateResourceRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateResourceRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateRole round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "is_global": true, "name": "example"})json");

    const auto value = wire.get<UpdateRole>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateRole>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateScopeRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "name": "example"})json");

    const auto value = wire.get<UpdateScopeRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateScopeRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateServiceAccount round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"description": "example", "name": "example", "status": "Active"})json");

    const auto value = wire.get<UpdateServiceAccount>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateServiceAccount>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateTenant round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"metadata": {}, "name": "example", "slug": "example", "status": "Active"})json");

    const auto value = wire.get<UpdateTenant>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateTenant>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateUserRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"email": "example", "metadata": {}, "status": "Active", "username": "example"})json");

    const auto value = wire.get<UpdateUserRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateUserRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model UpdateWebhookRequest round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"enabled": true, "events": ["example"], "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "secret": "example", "url": "example"})json");

    const auto value = wire.get<UpdateWebhookRequest>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<UpdateWebhookRequest>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model WebauthnAttestationPolicy round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"allowed_aaguids": ["11111111-1111-4111-8111-111111111111"], "block_revoked_status": true, "blocked_aaguids": ["11111111-1111-4111-8111-111111111111"], "min_certification": "L1", "mode": "none", "require_fido_certified": true, "unknown_aaguid": "allow"})json");

    const auto value = wire.get<WebauthnAttestationPolicy>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<WebauthnAttestationPolicy>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management model WebhookResponse round-trips without losing a field") {
    const auto wire = nlohmann::json::parse(
        R"json({"created_at": "2026-08-26T00:00:00Z", "enabled": true, "events": ["example"], "id": "11111111-1111-4111-8111-111111111111", "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "url": "example"})json");

    const auto value = wire.get<WebhookResponse>();
    const nlohmann::json encoded = value;
    // Exact equality, not "the fields I remembered to check". The wire object above carries
    // every property the spec declares, so a dropped field and an invented one both fail here.
    AXIAM_CHECK(encoded == wire);

    // And encoding is a fixed point -- a second pass changes nothing.
    const nlohmann::json again = encoded.get<WebhookResponse>();
    AXIAM_CHECK(again == encoded);
}

AXIAM_TEST("management enum ActorType maps every value both ways") {
    AXIAM_CHECK(to_wire(ActorType::User) == "User");
    AXIAM_CHECK(actor_type_from_wire("User") == ActorType::User);
    AXIAM_CHECK(to_wire(ActorType::ServiceAccount) == "ServiceAccount");
    AXIAM_CHECK(actor_type_from_wire("ServiceAccount") == ActorType::ServiceAccount);
    AXIAM_CHECK(to_wire(ActorType::System) == "System");
    AXIAM_CHECK(actor_type_from_wire("System") == ActorType::System);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(actor_type_from_wire("__not_a_actor_type__") == ActorType::Unknown);
    AXIAM_CHECK(ActorType::Unknown != ActorType::User);
    AXIAM_CHECK(ActorType::Unknown != ActorType::ServiceAccount);
    AXIAM_CHECK(ActorType::Unknown != ActorType::System);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(ActorType::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = ActorType::User;
    AXIAM_CHECK(j.get<std::string>() == "User");
    AXIAM_CHECK(j.get<ActorType>() == ActorType::User);
}

AXIAM_TEST("management enum AttestationMode maps every value both ways") {
    AXIAM_CHECK(to_wire(AttestationMode::None) == "none");
    AXIAM_CHECK(attestation_mode_from_wire("none") == AttestationMode::None);
    AXIAM_CHECK(to_wire(AttestationMode::Indirect) == "indirect");
    AXIAM_CHECK(attestation_mode_from_wire("indirect") == AttestationMode::Indirect);
    AXIAM_CHECK(to_wire(AttestationMode::DirectRequired) == "direct_required");
    AXIAM_CHECK(attestation_mode_from_wire("direct_required") == AttestationMode::DirectRequired);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(attestation_mode_from_wire("__not_a_attestation_mode__") == AttestationMode::Unknown);
    AXIAM_CHECK(AttestationMode::Unknown != AttestationMode::None);
    AXIAM_CHECK(AttestationMode::Unknown != AttestationMode::Indirect);
    AXIAM_CHECK(AttestationMode::Unknown != AttestationMode::DirectRequired);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(AttestationMode::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = AttestationMode::None;
    AXIAM_CHECK(j.get<std::string>() == "none");
    AXIAM_CHECK(j.get<AttestationMode>() == AttestationMode::None);
}

AXIAM_TEST("management enum AuditOutcome maps every value both ways") {
    AXIAM_CHECK(to_wire(AuditOutcome::Success) == "Success");
    AXIAM_CHECK(audit_outcome_from_wire("Success") == AuditOutcome::Success);
    AXIAM_CHECK(to_wire(AuditOutcome::Failure) == "Failure");
    AXIAM_CHECK(audit_outcome_from_wire("Failure") == AuditOutcome::Failure);
    AXIAM_CHECK(to_wire(AuditOutcome::Denied) == "Denied");
    AXIAM_CHECK(audit_outcome_from_wire("Denied") == AuditOutcome::Denied);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(audit_outcome_from_wire("__not_a_audit_outcome__") == AuditOutcome::Unknown);
    AXIAM_CHECK(AuditOutcome::Unknown != AuditOutcome::Success);
    AXIAM_CHECK(AuditOutcome::Unknown != AuditOutcome::Failure);
    AXIAM_CHECK(AuditOutcome::Unknown != AuditOutcome::Denied);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(AuditOutcome::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = AuditOutcome::Success;
    AXIAM_CHECK(j.get<std::string>() == "Success");
    AXIAM_CHECK(j.get<AuditOutcome>() == AuditOutcome::Success);
}

AXIAM_TEST("management enum CertificateStatus maps every value both ways") {
    AXIAM_CHECK(to_wire(CertificateStatus::Active) == "Active");
    AXIAM_CHECK(certificate_status_from_wire("Active") == CertificateStatus::Active);
    AXIAM_CHECK(to_wire(CertificateStatus::Revoked) == "Revoked");
    AXIAM_CHECK(certificate_status_from_wire("Revoked") == CertificateStatus::Revoked);
    AXIAM_CHECK(to_wire(CertificateStatus::Expired) == "Expired");
    AXIAM_CHECK(certificate_status_from_wire("Expired") == CertificateStatus::Expired);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(certificate_status_from_wire("__not_a_certificate_status__") == CertificateStatus::Unknown);
    AXIAM_CHECK(CertificateStatus::Unknown != CertificateStatus::Active);
    AXIAM_CHECK(CertificateStatus::Unknown != CertificateStatus::Revoked);
    AXIAM_CHECK(CertificateStatus::Unknown != CertificateStatus::Expired);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(CertificateStatus::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = CertificateStatus::Active;
    AXIAM_CHECK(j.get<std::string>() == "Active");
    AXIAM_CHECK(j.get<CertificateStatus>() == CertificateStatus::Active);
}

AXIAM_TEST("management enum CertificateType maps every value both ways") {
    AXIAM_CHECK(to_wire(CertificateType::User) == "User");
    AXIAM_CHECK(certificate_type_from_wire("User") == CertificateType::User);
    AXIAM_CHECK(to_wire(CertificateType::Service) == "Service");
    AXIAM_CHECK(certificate_type_from_wire("Service") == CertificateType::Service);
    AXIAM_CHECK(to_wire(CertificateType::Device) == "Device");
    AXIAM_CHECK(certificate_type_from_wire("Device") == CertificateType::Device);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(certificate_type_from_wire("__not_a_certificate_type__") == CertificateType::Unknown);
    AXIAM_CHECK(CertificateType::Unknown != CertificateType::User);
    AXIAM_CHECK(CertificateType::Unknown != CertificateType::Service);
    AXIAM_CHECK(CertificateType::Unknown != CertificateType::Device);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(CertificateType::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = CertificateType::User;
    AXIAM_CHECK(j.get<std::string>() == "User");
    AXIAM_CHECK(j.get<CertificateType>() == CertificateType::User);
}

AXIAM_TEST("management enum CertificationLevel maps every value both ways") {
    AXIAM_CHECK(to_wire(CertificationLevel::L1) == "L1");
    AXIAM_CHECK(certification_level_from_wire("L1") == CertificationLevel::L1);
    AXIAM_CHECK(to_wire(CertificationLevel::L1Plus) == "L1Plus");
    AXIAM_CHECK(certification_level_from_wire("L1Plus") == CertificationLevel::L1Plus);
    AXIAM_CHECK(to_wire(CertificationLevel::L2) == "L2");
    AXIAM_CHECK(certification_level_from_wire("L2") == CertificationLevel::L2);
    AXIAM_CHECK(to_wire(CertificationLevel::L2Plus) == "L2Plus");
    AXIAM_CHECK(certification_level_from_wire("L2Plus") == CertificationLevel::L2Plus);
    AXIAM_CHECK(to_wire(CertificationLevel::L3) == "L3");
    AXIAM_CHECK(certification_level_from_wire("L3") == CertificationLevel::L3);
    AXIAM_CHECK(to_wire(CertificationLevel::L3Plus) == "L3Plus");
    AXIAM_CHECK(certification_level_from_wire("L3Plus") == CertificationLevel::L3Plus);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(certification_level_from_wire("__not_a_certification_level__") == CertificationLevel::Unknown);
    AXIAM_CHECK(CertificationLevel::Unknown != CertificationLevel::L1);
    AXIAM_CHECK(CertificationLevel::Unknown != CertificationLevel::L1Plus);
    AXIAM_CHECK(CertificationLevel::Unknown != CertificationLevel::L2);
    AXIAM_CHECK(CertificationLevel::Unknown != CertificationLevel::L2Plus);
    AXIAM_CHECK(CertificationLevel::Unknown != CertificationLevel::L3);
    AXIAM_CHECK(CertificationLevel::Unknown != CertificationLevel::L3Plus);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(CertificationLevel::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = CertificationLevel::L1;
    AXIAM_CHECK(j.get<std::string>() == "L1");
    AXIAM_CHECK(j.get<CertificationLevel>() == CertificationLevel::L1);
}

AXIAM_TEST("management enum ClientAuthMethod maps every value both ways") {
    AXIAM_CHECK(to_wire(ClientAuthMethod::ClientSecretPost) == "client_secret_post");
    AXIAM_CHECK(client_auth_method_from_wire("client_secret_post") == ClientAuthMethod::ClientSecretPost);
    AXIAM_CHECK(to_wire(ClientAuthMethod::TlsClientAuth) == "tls_client_auth");
    AXIAM_CHECK(client_auth_method_from_wire("tls_client_auth") == ClientAuthMethod::TlsClientAuth);
    AXIAM_CHECK(to_wire(ClientAuthMethod::SelfSignedTlsClientAuth) == "self_signed_tls_client_auth");
    AXIAM_CHECK(client_auth_method_from_wire("self_signed_tls_client_auth") == ClientAuthMethod::SelfSignedTlsClientAuth);
    AXIAM_CHECK(to_wire(ClientAuthMethod::PrivateKeyJwt) == "private_key_jwt");
    AXIAM_CHECK(client_auth_method_from_wire("private_key_jwt") == ClientAuthMethod::PrivateKeyJwt);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(client_auth_method_from_wire("__not_a_client_auth_method__") == ClientAuthMethod::Unknown);
    AXIAM_CHECK(ClientAuthMethod::Unknown != ClientAuthMethod::ClientSecretPost);
    AXIAM_CHECK(ClientAuthMethod::Unknown != ClientAuthMethod::TlsClientAuth);
    AXIAM_CHECK(ClientAuthMethod::Unknown != ClientAuthMethod::SelfSignedTlsClientAuth);
    AXIAM_CHECK(ClientAuthMethod::Unknown != ClientAuthMethod::PrivateKeyJwt);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(ClientAuthMethod::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = ClientAuthMethod::ClientSecretPost;
    AXIAM_CHECK(j.get<std::string>() == "client_secret_post");
    AXIAM_CHECK(j.get<ClientAuthMethod>() == ClientAuthMethod::ClientSecretPost);
}

AXIAM_TEST("management enum ClientProfile maps every value both ways") {
    AXIAM_CHECK(to_wire(ClientProfile::Standard) == "standard");
    AXIAM_CHECK(client_profile_from_wire("standard") == ClientProfile::Standard);
    AXIAM_CHECK(to_wire(ClientProfile::Fapi2) == "fapi2");
    AXIAM_CHECK(client_profile_from_wire("fapi2") == ClientProfile::Fapi2);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(client_profile_from_wire("__not_a_client_profile__") == ClientProfile::Unknown);
    AXIAM_CHECK(ClientProfile::Unknown != ClientProfile::Standard);
    AXIAM_CHECK(ClientProfile::Unknown != ClientProfile::Fapi2);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(ClientProfile::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = ClientProfile::Standard;
    AXIAM_CHECK(j.get<std::string>() == "standard");
    AXIAM_CHECK(j.get<ClientProfile>() == ClientProfile::Standard);
}

AXIAM_TEST("management enum FailurePolicy maps every value both ways") {
    AXIAM_CHECK(to_wire(FailurePolicy::FailClosed) == "fail_closed");
    AXIAM_CHECK(failure_policy_from_wire("fail_closed") == FailurePolicy::FailClosed);
    AXIAM_CHECK(to_wire(FailurePolicy::FailOpen) == "fail_open");
    AXIAM_CHECK(failure_policy_from_wire("fail_open") == FailurePolicy::FailOpen);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(failure_policy_from_wire("__not_a_failure_policy__") == FailurePolicy::Unknown);
    AXIAM_CHECK(FailurePolicy::Unknown != FailurePolicy::FailClosed);
    AXIAM_CHECK(FailurePolicy::Unknown != FailurePolicy::FailOpen);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(FailurePolicy::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = FailurePolicy::FailClosed;
    AXIAM_CHECK(j.get<std::string>() == "fail_closed");
    AXIAM_CHECK(j.get<FailurePolicy>() == FailurePolicy::FailClosed);
}

AXIAM_TEST("management enum KeyAlgorithm maps every value both ways") {
    AXIAM_CHECK(to_wire(KeyAlgorithm::Rsa4096) == "Rsa4096");
    AXIAM_CHECK(key_algorithm_from_wire("Rsa4096") == KeyAlgorithm::Rsa4096);
    AXIAM_CHECK(to_wire(KeyAlgorithm::Ed25519) == "Ed25519");
    AXIAM_CHECK(key_algorithm_from_wire("Ed25519") == KeyAlgorithm::Ed25519);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(key_algorithm_from_wire("__not_a_key_algorithm__") == KeyAlgorithm::Unknown);
    AXIAM_CHECK(KeyAlgorithm::Unknown != KeyAlgorithm::Rsa4096);
    AXIAM_CHECK(KeyAlgorithm::Unknown != KeyAlgorithm::Ed25519);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(KeyAlgorithm::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = KeyAlgorithm::Rsa4096;
    AXIAM_CHECK(j.get<std::string>() == "Rsa4096");
    AXIAM_CHECK(j.get<KeyAlgorithm>() == KeyAlgorithm::Rsa4096);
}

AXIAM_TEST("management enum MfaMethodType maps every value both ways") {
    AXIAM_CHECK(to_wire(MfaMethodType::Totp) == "Totp");
    AXIAM_CHECK(mfa_method_type_from_wire("Totp") == MfaMethodType::Totp);
    AXIAM_CHECK(to_wire(MfaMethodType::Passkey) == "Passkey");
    AXIAM_CHECK(mfa_method_type_from_wire("Passkey") == MfaMethodType::Passkey);
    AXIAM_CHECK(to_wire(MfaMethodType::SecurityKey) == "SecurityKey");
    AXIAM_CHECK(mfa_method_type_from_wire("SecurityKey") == MfaMethodType::SecurityKey);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(mfa_method_type_from_wire("__not_a_mfa_method_type__") == MfaMethodType::Unknown);
    AXIAM_CHECK(MfaMethodType::Unknown != MfaMethodType::Totp);
    AXIAM_CHECK(MfaMethodType::Unknown != MfaMethodType::Passkey);
    AXIAM_CHECK(MfaMethodType::Unknown != MfaMethodType::SecurityKey);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(MfaMethodType::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = MfaMethodType::Totp;
    AXIAM_CHECK(j.get<std::string>() == "Totp");
    AXIAM_CHECK(j.get<MfaMethodType>() == MfaMethodType::Totp);
}

AXIAM_TEST("management enum NotificationEventType maps every value both ways") {
    AXIAM_CHECK(to_wire(NotificationEventType::LoginFailure) == "login_failure");
    AXIAM_CHECK(notification_event_type_from_wire("login_failure") == NotificationEventType::LoginFailure);
    AXIAM_CHECK(to_wire(NotificationEventType::AccountLocked) == "account_locked");
    AXIAM_CHECK(notification_event_type_from_wire("account_locked") == NotificationEventType::AccountLocked);
    AXIAM_CHECK(to_wire(NotificationEventType::MfaEnrollmentChanged) == "mfa_enrollment_changed");
    AXIAM_CHECK(notification_event_type_from_wire("mfa_enrollment_changed") == NotificationEventType::MfaEnrollmentChanged);
    AXIAM_CHECK(to_wire(NotificationEventType::PasswordChanged) == "password_changed");
    AXIAM_CHECK(notification_event_type_from_wire("password_changed") == NotificationEventType::PasswordChanged);
    AXIAM_CHECK(to_wire(NotificationEventType::PasswordResetRequested) == "password_reset_requested");
    AXIAM_CHECK(notification_event_type_from_wire("password_reset_requested") == NotificationEventType::PasswordResetRequested);
    AXIAM_CHECK(to_wire(NotificationEventType::RoleAssigned) == "role_assigned");
    AXIAM_CHECK(notification_event_type_from_wire("role_assigned") == NotificationEventType::RoleAssigned);
    AXIAM_CHECK(to_wire(NotificationEventType::RoleUnassigned) == "role_unassigned");
    AXIAM_CHECK(notification_event_type_from_wire("role_unassigned") == NotificationEventType::RoleUnassigned);
    AXIAM_CHECK(to_wire(NotificationEventType::PermissionGranted) == "permission_granted");
    AXIAM_CHECK(notification_event_type_from_wire("permission_granted") == NotificationEventType::PermissionGranted);
    AXIAM_CHECK(to_wire(NotificationEventType::PermissionRevoked) == "permission_revoked");
    AXIAM_CHECK(notification_event_type_from_wire("permission_revoked") == NotificationEventType::PermissionRevoked);
    AXIAM_CHECK(to_wire(NotificationEventType::CertificateIssued) == "certificate_issued");
    AXIAM_CHECK(notification_event_type_from_wire("certificate_issued") == NotificationEventType::CertificateIssued);
    AXIAM_CHECK(to_wire(NotificationEventType::CertificateRevoked) == "certificate_revoked");
    AXIAM_CHECK(notification_event_type_from_wire("certificate_revoked") == NotificationEventType::CertificateRevoked);
    AXIAM_CHECK(to_wire(NotificationEventType::CaCertificateRevoked) == "ca_certificate_revoked");
    AXIAM_CHECK(notification_event_type_from_wire("ca_certificate_revoked") == NotificationEventType::CaCertificateRevoked);
    AXIAM_CHECK(to_wire(NotificationEventType::UserCreated) == "user_created");
    AXIAM_CHECK(notification_event_type_from_wire("user_created") == NotificationEventType::UserCreated);
    AXIAM_CHECK(to_wire(NotificationEventType::UserDeleted) == "user_deleted");
    AXIAM_CHECK(notification_event_type_from_wire("user_deleted") == NotificationEventType::UserDeleted);
    AXIAM_CHECK(to_wire(NotificationEventType::UserUpdated) == "user_updated");
    AXIAM_CHECK(notification_event_type_from_wire("user_updated") == NotificationEventType::UserUpdated);
    AXIAM_CHECK(to_wire(NotificationEventType::ServiceAccountCreated) == "service_account_created");
    AXIAM_CHECK(notification_event_type_from_wire("service_account_created") == NotificationEventType::ServiceAccountCreated);
    AXIAM_CHECK(to_wire(NotificationEventType::ServiceAccountDeleted) == "service_account_deleted");
    AXIAM_CHECK(notification_event_type_from_wire("service_account_deleted") == NotificationEventType::ServiceAccountDeleted);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(notification_event_type_from_wire("__not_a_notification_event_type__") == NotificationEventType::Unknown);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::LoginFailure);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::AccountLocked);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::MfaEnrollmentChanged);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::PasswordChanged);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::PasswordResetRequested);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::RoleAssigned);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::RoleUnassigned);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::PermissionGranted);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::PermissionRevoked);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::CertificateIssued);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::CertificateRevoked);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::CaCertificateRevoked);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::UserCreated);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::UserDeleted);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::UserUpdated);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::ServiceAccountCreated);
    AXIAM_CHECK(NotificationEventType::Unknown != NotificationEventType::ServiceAccountDeleted);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(NotificationEventType::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = NotificationEventType::LoginFailure;
    AXIAM_CHECK(j.get<std::string>() == "login_failure");
    AXIAM_CHECK(j.get<NotificationEventType>() == NotificationEventType::LoginFailure);
}

AXIAM_TEST("management enum PermissionEffect maps every value both ways") {
    AXIAM_CHECK(to_wire(PermissionEffect::Allow) == "allow");
    AXIAM_CHECK(permission_effect_from_wire("allow") == PermissionEffect::Allow);
    AXIAM_CHECK(to_wire(PermissionEffect::Deny) == "deny");
    AXIAM_CHECK(permission_effect_from_wire("deny") == PermissionEffect::Deny);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(permission_effect_from_wire("__not_a_permission_effect__") == PermissionEffect::Unknown);
    AXIAM_CHECK(PermissionEffect::Unknown != PermissionEffect::Allow);
    AXIAM_CHECK(PermissionEffect::Unknown != PermissionEffect::Deny);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(PermissionEffect::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = PermissionEffect::Allow;
    AXIAM_CHECK(j.get<std::string>() == "allow");
    AXIAM_CHECK(j.get<PermissionEffect>() == PermissionEffect::Allow);
}

AXIAM_TEST("management enum PgpKeyAlgorithm maps every value both ways") {
    AXIAM_CHECK(to_wire(PgpKeyAlgorithm::Rsa4096) == "Rsa4096");
    AXIAM_CHECK(pgp_key_algorithm_from_wire("Rsa4096") == PgpKeyAlgorithm::Rsa4096);
    AXIAM_CHECK(to_wire(PgpKeyAlgorithm::Ed25519) == "Ed25519");
    AXIAM_CHECK(pgp_key_algorithm_from_wire("Ed25519") == PgpKeyAlgorithm::Ed25519);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(pgp_key_algorithm_from_wire("__not_a_pgp_key_algorithm__") == PgpKeyAlgorithm::Unknown);
    AXIAM_CHECK(PgpKeyAlgorithm::Unknown != PgpKeyAlgorithm::Rsa4096);
    AXIAM_CHECK(PgpKeyAlgorithm::Unknown != PgpKeyAlgorithm::Ed25519);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(PgpKeyAlgorithm::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = PgpKeyAlgorithm::Rsa4096;
    AXIAM_CHECK(j.get<std::string>() == "Rsa4096");
    AXIAM_CHECK(j.get<PgpKeyAlgorithm>() == PgpKeyAlgorithm::Rsa4096);
}

AXIAM_TEST("management enum PgpKeyPurpose maps every value both ways") {
    AXIAM_CHECK(to_wire(PgpKeyPurpose::AuditSigning) == "AuditSigning");
    AXIAM_CHECK(pgp_key_purpose_from_wire("AuditSigning") == PgpKeyPurpose::AuditSigning);
    AXIAM_CHECK(to_wire(PgpKeyPurpose::Export_) == "Export");
    AXIAM_CHECK(pgp_key_purpose_from_wire("Export") == PgpKeyPurpose::Export_);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(pgp_key_purpose_from_wire("__not_a_pgp_key_purpose__") == PgpKeyPurpose::Unknown);
    AXIAM_CHECK(PgpKeyPurpose::Unknown != PgpKeyPurpose::AuditSigning);
    AXIAM_CHECK(PgpKeyPurpose::Unknown != PgpKeyPurpose::Export_);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(PgpKeyPurpose::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = PgpKeyPurpose::AuditSigning;
    AXIAM_CHECK(j.get<std::string>() == "AuditSigning");
    AXIAM_CHECK(j.get<PgpKeyPurpose>() == PgpKeyPurpose::AuditSigning);
}

AXIAM_TEST("management enum PgpKeyStatus maps every value both ways") {
    AXIAM_CHECK(to_wire(PgpKeyStatus::Active) == "Active");
    AXIAM_CHECK(pgp_key_status_from_wire("Active") == PgpKeyStatus::Active);
    AXIAM_CHECK(to_wire(PgpKeyStatus::Revoked) == "Revoked");
    AXIAM_CHECK(pgp_key_status_from_wire("Revoked") == PgpKeyStatus::Revoked);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(pgp_key_status_from_wire("__not_a_pgp_key_status__") == PgpKeyStatus::Unknown);
    AXIAM_CHECK(PgpKeyStatus::Unknown != PgpKeyStatus::Active);
    AXIAM_CHECK(PgpKeyStatus::Unknown != PgpKeyStatus::Revoked);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(PgpKeyStatus::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = PgpKeyStatus::Active;
    AXIAM_CHECK(j.get<std::string>() == "Active");
    AXIAM_CHECK(j.get<PgpKeyStatus>() == PgpKeyStatus::Active);
}

AXIAM_TEST("management enum ReactorMode maps every value both ways") {
    AXIAM_CHECK(to_wire(ReactorMode::Intercept) == "intercept");
    AXIAM_CHECK(reactor_mode_from_wire("intercept") == ReactorMode::Intercept);
    AXIAM_CHECK(to_wire(ReactorMode::Listen) == "listen");
    AXIAM_CHECK(reactor_mode_from_wire("listen") == ReactorMode::Listen);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(reactor_mode_from_wire("__not_a_reactor_mode__") == ReactorMode::Unknown);
    AXIAM_CHECK(ReactorMode::Unknown != ReactorMode::Intercept);
    AXIAM_CHECK(ReactorMode::Unknown != ReactorMode::Listen);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(ReactorMode::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = ReactorMode::Intercept;
    AXIAM_CHECK(j.get<std::string>() == "intercept");
    AXIAM_CHECK(j.get<ReactorMode>() == ReactorMode::Intercept);
}

AXIAM_TEST("management enum ScimTokenStatus maps every value both ways") {
    AXIAM_CHECK(to_wire(ScimTokenStatus::Active) == "active");
    AXIAM_CHECK(scim_token_status_from_wire("active") == ScimTokenStatus::Active);
    AXIAM_CHECK(to_wire(ScimTokenStatus::Expired) == "expired");
    AXIAM_CHECK(scim_token_status_from_wire("expired") == ScimTokenStatus::Expired);
    AXIAM_CHECK(to_wire(ScimTokenStatus::Revoked) == "revoked");
    AXIAM_CHECK(scim_token_status_from_wire("revoked") == ScimTokenStatus::Revoked);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(scim_token_status_from_wire("__not_a_scim_token_status__") == ScimTokenStatus::Unknown);
    AXIAM_CHECK(ScimTokenStatus::Unknown != ScimTokenStatus::Active);
    AXIAM_CHECK(ScimTokenStatus::Unknown != ScimTokenStatus::Expired);
    AXIAM_CHECK(ScimTokenStatus::Unknown != ScimTokenStatus::Revoked);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(ScimTokenStatus::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = ScimTokenStatus::Active;
    AXIAM_CHECK(j.get<std::string>() == "active");
    AXIAM_CHECK(j.get<ScimTokenStatus>() == ScimTokenStatus::Active);
}

AXIAM_TEST("management enum SettingsScope maps every value both ways") {
    AXIAM_CHECK(to_wire(SettingsScope::Org) == "Org");
    AXIAM_CHECK(settings_scope_from_wire("Org") == SettingsScope::Org);
    AXIAM_CHECK(to_wire(SettingsScope::Tenant) == "Tenant");
    AXIAM_CHECK(settings_scope_from_wire("Tenant") == SettingsScope::Tenant);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(settings_scope_from_wire("__not_a_settings_scope__") == SettingsScope::Unknown);
    AXIAM_CHECK(SettingsScope::Unknown != SettingsScope::Org);
    AXIAM_CHECK(SettingsScope::Unknown != SettingsScope::Tenant);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(SettingsScope::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = SettingsScope::Org;
    AXIAM_CHECK(j.get<std::string>() == "Org");
    AXIAM_CHECK(j.get<SettingsScope>() == SettingsScope::Org);
}

AXIAM_TEST("management enum TenantKind maps every value both ways") {
    AXIAM_CHECK(to_wire(TenantKind::Standard) == "standard");
    AXIAM_CHECK(tenant_kind_from_wire("standard") == TenantKind::Standard);
    AXIAM_CHECK(to_wire(TenantKind::Organization) == "organization");
    AXIAM_CHECK(tenant_kind_from_wire("organization") == TenantKind::Organization);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(tenant_kind_from_wire("__not_a_tenant_kind__") == TenantKind::Unknown);
    AXIAM_CHECK(TenantKind::Unknown != TenantKind::Standard);
    AXIAM_CHECK(TenantKind::Unknown != TenantKind::Organization);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(TenantKind::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = TenantKind::Standard;
    AXIAM_CHECK(j.get<std::string>() == "standard");
    AXIAM_CHECK(j.get<TenantKind>() == TenantKind::Standard);
}

AXIAM_TEST("management enum TenantStatus maps every value both ways") {
    AXIAM_CHECK(to_wire(TenantStatus::Active) == "Active");
    AXIAM_CHECK(tenant_status_from_wire("Active") == TenantStatus::Active);
    AXIAM_CHECK(to_wire(TenantStatus::Suspended) == "Suspended");
    AXIAM_CHECK(tenant_status_from_wire("Suspended") == TenantStatus::Suspended);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(tenant_status_from_wire("__not_a_tenant_status__") == TenantStatus::Unknown);
    AXIAM_CHECK(TenantStatus::Unknown != TenantStatus::Active);
    AXIAM_CHECK(TenantStatus::Unknown != TenantStatus::Suspended);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(TenantStatus::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = TenantStatus::Active;
    AXIAM_CHECK(j.get<std::string>() == "Active");
    AXIAM_CHECK(j.get<TenantStatus>() == TenantStatus::Active);
}

AXIAM_TEST("management enum UnknownAaguidAction maps every value both ways") {
    AXIAM_CHECK(to_wire(UnknownAaguidAction::Allow) == "allow");
    AXIAM_CHECK(unknown_aaguid_action_from_wire("allow") == UnknownAaguidAction::Allow);
    AXIAM_CHECK(to_wire(UnknownAaguidAction::Deny) == "deny");
    AXIAM_CHECK(unknown_aaguid_action_from_wire("deny") == UnknownAaguidAction::Deny);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(unknown_aaguid_action_from_wire("__not_a_unknown_aaguid_action__") == UnknownAaguidAction::Unknown);
    AXIAM_CHECK(UnknownAaguidAction::Unknown != UnknownAaguidAction::Allow);
    AXIAM_CHECK(UnknownAaguidAction::Unknown != UnknownAaguidAction::Deny);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(UnknownAaguidAction::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = UnknownAaguidAction::Allow;
    AXIAM_CHECK(j.get<std::string>() == "allow");
    AXIAM_CHECK(j.get<UnknownAaguidAction>() == UnknownAaguidAction::Allow);
}

AXIAM_TEST("management enum UserStatus maps every value both ways") {
    AXIAM_CHECK(to_wire(UserStatus::Active) == "Active");
    AXIAM_CHECK(user_status_from_wire("Active") == UserStatus::Active);
    AXIAM_CHECK(to_wire(UserStatus::Inactive) == "Inactive");
    AXIAM_CHECK(user_status_from_wire("Inactive") == UserStatus::Inactive);
    AXIAM_CHECK(to_wire(UserStatus::Locked) == "Locked");
    AXIAM_CHECK(user_status_from_wire("Locked") == UserStatus::Locked);
    AXIAM_CHECK(to_wire(UserStatus::PendingVerification) == "PendingVerification");
    AXIAM_CHECK(user_status_from_wire("PendingVerification") == UserStatus::PendingVerification);
    AXIAM_CHECK(to_wire(UserStatus::Anonymized) == "Anonymized");
    AXIAM_CHECK(user_status_from_wire("Anonymized") == UserStatus::Anonymized);
    AXIAM_CHECK(to_wire(UserStatus::Deleted) == "Deleted");
    AXIAM_CHECK(user_status_from_wire("Deleted") == UserStatus::Deleted);

    // An unrecognised value DECODES, to an enumerator of its own -- it is not reported, and it
    // is not mapped to whichever known enumerator happens to be first (§27.11 rule 1). Throwing
    // here would fail the whole response the value arrived in, so one field of one record would
    // take down the page it was on, including the records the caller did ask for.
    AXIAM_CHECK(user_status_from_wire("__not_a_user_status__") == UserStatus::Unknown);
    AXIAM_CHECK(UserStatus::Unknown != UserStatus::Active);
    AXIAM_CHECK(UserStatus::Unknown != UserStatus::Inactive);
    AXIAM_CHECK(UserStatus::Unknown != UserStatus::Locked);
    AXIAM_CHECK(UserStatus::Unknown != UserStatus::PendingVerification);
    AXIAM_CHECK(UserStatus::Unknown != UserStatus::Anonymized);
    AXIAM_CHECK(UserStatus::Unknown != UserStatus::Deleted);
    // The empty string, which no server value is: an unrecognised value carried back into an
    // update is refused by the server rather than written as a spelling it never used.
    AXIAM_CHECK(to_wire(UserStatus::Unknown).empty());

    // The JSON hooks must agree with the wire functions, or a model carrying this enum encodes
    // differently from the enum itself.
    const nlohmann::json j = UserStatus::Active;
    AXIAM_CHECK(j.get<std::string>() == "Active");
    AXIAM_CHECK(j.get<UserStatus>() == UserStatus::Active);
}

AXIAM_TEST("management organizations: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "slug": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "slug": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.organizations();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations");
}

AXIAM_TEST("management tenants: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "kind": "standard", "metadata": {}, "name": "example", "organization_id": "11111111-1111-4111-8111-111111111111", "slug": "example", "status": "Active", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "kind": "standard", "metadata": {}, "name": "example", "organization_id": "11111111-1111-4111-8111-111111111111", "slug": "example", "status": "Active", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.tenants();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes at least one of the two.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/22222222-2222-4222-8222-222222222222/tenants");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/tenants");
}

AXIAM_TEST("management users: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "email": "example", "email_verified": true, "failed_login_attempts": 1, "id": "11111111-1111-4111-8111-111111111111", "is_locked": true, "locked_until": "2026-08-26T00:00:00Z", "metadata": {}, "mfa_enabled": true, "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "username": "example"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "email": "example", "email_verified": true, "failed_login_attempts": 1, "id": "11111111-1111-4111-8111-111111111111", "is_locked": true, "locked_until": "2026-08-26T00:00:00Z", "metadata": {}, "mfa_enabled": true, "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "username": "example"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.users();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/users");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/users");
}

AXIAM_TEST("management groups: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.groups();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/groups");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/groups");
}

AXIAM_TEST("management roles: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "is_global": true, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "is_global": true, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.roles();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/roles");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/roles");
}

AXIAM_TEST("management permissions: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"action": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"action": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.permissions();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/permissions");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/permissions");
}

AXIAM_TEST("management resources: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "uma_registered_by": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "uma_registered_by": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.resources();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/resources");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/resources");
}

AXIAM_TEST("management scopes: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json([{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}])json",
                                              200, R"json([{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}])json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.scopes();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list("11111111-1111-4111-8111-111111111111");
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/resources/11111111-1111-4111-8111-111111111111/scopes");

    // The original still points where it did.
    (void) handle.list("11111111-1111-4111-8111-111111111111");
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/resources/11111111-1111-4111-8111-111111111111/scopes");
}

AXIAM_TEST("management service_accounts: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.service_accounts();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/service-accounts");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/service-accounts");
}

AXIAM_TEST("management certificates: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"cert_type": "User", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"cert_type": "User", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.certificates();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/certificates");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/certificates");
}

AXIAM_TEST("management ca_certificates: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "key_custody": "example", "key_locator": "example", "mtls_trust_anchor": true, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "organization_id": "11111111-1111-4111-8111-111111111111", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "key_custody": "example", "key_locator": "example", "mtls_trust_anchor": true, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "organization_id": "11111111-1111-4111-8111-111111111111", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.ca_certificates();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes at least one of the two.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/22222222-2222-4222-8222-222222222222/ca-certificates");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/ca-certificates");
}

AXIAM_TEST("management pgp_keys: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"algorithm": "Rsa4096", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "public_key_armored": "example", "purpose": "AuditSigning", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"algorithm": "Rsa4096", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "public_key_armored": "example", "purpose": "AuditSigning", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.pgp_keys();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/pgp-keys");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/pgp-keys");
}

AXIAM_TEST("management webhooks: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "enabled": true, "events": ["example"], "id": "11111111-1111-4111-8111-111111111111", "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "url": "example"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "enabled": true, "events": ["example"], "id": "11111111-1111-4111-8111-111111111111", "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "url": "example"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.webhooks();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/webhooks");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/webhooks");
}

AXIAM_TEST("management oauth2_clients: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "id": "11111111-1111-4111-8111-111111111111", "jwks": "example", "jwks_uri": "example", "name": "example", "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "id": "11111111-1111-4111-8111-111111111111", "jwks": "example", "jwks_uri": "example", "name": "example", "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.oauth2_clients();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/oauth2-clients");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/oauth2-clients");
}

AXIAM_TEST("management federation: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"attribute_map": {}, "client_id": "example", "created_at": "2026-08-26T00:00:00Z", "enabled": true, "id": "11111111-1111-4111-8111-111111111111", "metadata_url": "example", "protocol": "example", "provider": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"attribute_map": {}, "client_id": "example", "created_at": "2026-08-26T00:00:00Z", "enabled": true, "id": "11111111-1111-4111-8111-111111111111", "metadata_url": "example", "protocol": "example", "provider": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.federation();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list_configs();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/federation-configs");

    // The original still points where it did.
    (void) handle.list_configs();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/federation-configs");
}

AXIAM_TEST("management notification_rules: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["login_failure"], "id": "11111111-1111-4111-8111-111111111111", "name": "example", "recipient_emails": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["login_failure"], "id": "11111111-1111-4111-8111-111111111111", "name": "example", "recipient_emails": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.notification_rules();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/notification-rules");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/notification-rules");
}

AXIAM_TEST("management email_config: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"created_at": "2026-08-26T00:00:00Z", "enabled": true, "from_email": "example", "from_name": "example", "id": "11111111-1111-4111-8111-111111111111", "provider": {"kind": "smtp", "host": "example", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example", "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json",
                                              200, R"json({"created_at": "2026-08-26T00:00:00Z", "enabled": true, "from_email": "example", "from_name": "example", "id": "11111111-1111-4111-8111-111111111111", "provider": {"kind": "smtp", "host": "example", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example", "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.email_config();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.get_org();
    // This route substitutes at least one of the two.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/22222222-2222-4222-8222-222222222222/email-config");

    // The original still points where it did.
    (void) handle.get_org();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/email-config");
}

AXIAM_TEST("management settings: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"certificate": {"default_cert_validity_days": 1, "max_cert_validity_days": 1}, "created_at": "2026-08-26T00:00:00Z", "email": {"email_verification_grace_period_hours": 1, "email_verification_required": true}, "id": "11111111-1111-4111-8111-111111111111", "lockout": {"lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1}, "mfa": {"mfa_challenge_lifetime_secs": 1, "mfa_enforced": true}, "notification": {"admin_notifications_enabled": true}, "opaque": {"opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example"}, "password": {"hibp_check_enabled": true, "min_length": 1, "password_history_count": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true}, "privacy": {"deletion_grace_period_days": 1}, "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "token": {"access_token_lifetime_secs": 1, "refresh_token_lifetime_secs": 1}, "updated_at": "2026-08-26T00:00:00Z"})json",
                                              200, R"json({"certificate": {"default_cert_validity_days": 1, "max_cert_validity_days": 1}, "created_at": "2026-08-26T00:00:00Z", "email": {"email_verification_grace_period_hours": 1, "email_verification_required": true}, "id": "11111111-1111-4111-8111-111111111111", "lockout": {"lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1}, "mfa": {"mfa_challenge_lifetime_secs": 1, "mfa_enforced": true}, "notification": {"admin_notifications_enabled": true}, "opaque": {"opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example"}, "password": {"hibp_check_enabled": true, "min_length": 1, "password_history_count": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true}, "privacy": {"deletion_grace_period_days": 1}, "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "token": {"access_token_lifetime_secs": 1, "refresh_token_lifetime_secs": 1}, "updated_at": "2026-08-26T00:00:00Z"})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.settings();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.get_org();
    // This route substitutes at least one of the two.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/22222222-2222-4222-8222-222222222222/settings");

    // The original still points where it did.
    (void) handle.get_org();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/settings");
}

AXIAM_TEST("management scim_tokens: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json([{"created_at": "2026-08-26T00:00:00Z", "created_by": "11111111-1111-4111-8111-111111111111", "expires_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "last_used_at": "2026-08-26T00:00:00Z", "name": "example", "revoked_at": "2026-08-26T00:00:00Z", "status": "active", "tenant_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"}])json",
                                              200, R"json([{"created_at": "2026-08-26T00:00:00Z", "created_by": "11111111-1111-4111-8111-111111111111", "expires_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "last_used_at": "2026-08-26T00:00:00Z", "name": "example", "revoked_at": "2026-08-26T00:00:00Z", "status": "active", "tenant_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"}])json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.scim_tokens();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/scim-tokens");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/scim-tokens");
}

AXIAM_TEST("management reactors: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "id": "11111111-1111-4111-8111-111111111111", "last_seen_at": "2026-08-26T00:00:00Z", "mode": "intercept", "name": "example", "priority": 1, "recent_timeout_count": 1, "recent_veto_count": 1, "tenant_id": "11111111-1111-4111-8111-111111111111", "timeout_ms": 1, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "id": "11111111-1111-4111-8111-111111111111", "last_seen_at": "2026-08-26T00:00:00Z", "mode": "intercept", "name": "example", "priority": 1, "recent_timeout_count": 1, "recent_veto_count": 1, "tenant_id": "11111111-1111-4111-8111-111111111111", "timeout_ms": 1, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.reactors();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/reactors");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/reactors");
}

AXIAM_TEST("management webauthn_policy: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"allowed_aaguids": ["11111111-1111-4111-8111-111111111111"], "block_revoked_status": true, "blocked_aaguids": ["11111111-1111-4111-8111-111111111111"], "min_certification": "L1", "mode": "none", "require_fido_certified": true, "unknown_aaguid": "allow", "effective_unknown_aaguid": "allow"})json",
                                              200, R"json({"allowed_aaguids": ["11111111-1111-4111-8111-111111111111"], "block_revoked_status": true, "blocked_aaguids": ["11111111-1111-4111-8111-111111111111"], "min_certification": "L1", "mode": "none", "require_fido_certified": true, "unknown_aaguid": "allow", "effective_unknown_aaguid": "allow"})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.webauthn_policy();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.get();
    // This route substitutes at least one of the two.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/tenants/33333333-3333-4333-8333-333333333333/webauthn/attestation-policy");

    // The original still points where it did.
    (void) handle.get();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/tenants/11111111-1111-4111-8111-111111111111/webauthn/attestation-policy");
}

AXIAM_TEST("management audit: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"action": "example", "actor_id": "11111111-1111-4111-8111-111111111111", "actor_type": "User", "id": "11111111-1111-4111-8111-111111111111", "ip_address": "example", "metadata": {}, "outcome": "Success", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "timestamp": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"action": "example", "actor_id": "11111111-1111-4111-8111-111111111111", "actor_type": "User", "id": "11111111-1111-4111-8111-111111111111", "ip_address": "example", "metadata": {}, "outcome": "Success", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "timestamp": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.audit();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.list();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/audit-logs");

    // The original still points where it did.
    (void) handle.list();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/audit-logs");
}

AXIAM_TEST("management privacy: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(204, "",
                                              204, "");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.privacy();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.request_export();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/account/export");

    // The original still points where it did.
    (void) handle.request_export();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/api/v1/account/export");
}

AXIAM_TEST("management platform: re-scoping returns a new handle (§27.4 rule 3)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"status": "example"})json",
                                              200, R"json({"status": "example"})json");
    auto mgmt = fixture.client.management();
    auto handle = mgmt.platform();

    // The re-scoped handle is a DIFFERENT object. On a management surface a handle that mutated
    // in place would not merely read the wrong tenant -- an unrelated code path re-scoping a
    // shared handle would WRITE to it.
    auto elsewhere = handle.in_org("22222222-2222-4222-8222-222222222222").for_tenant("33333333-3333-4333-8333-333333333333");
    (void) elsewhere.health();
    // This route substitutes NEITHER identifier, so the override is inert and the path is
    // unchanged -- which is the assertion.
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/health");

    // The original still points where it did.
    (void) handle.health();
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == "/health");
}

AXIAM_TEST("management organizations: client.organizations() and management().organizations() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "slug": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "slug": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.organizations().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().organizations().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/organizations");
}

AXIAM_TEST("management tenants: client.tenants() and management().tenants() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "kind": "standard", "metadata": {}, "name": "example", "organization_id": "11111111-1111-4111-8111-111111111111", "slug": "example", "status": "Active", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "kind": "standard", "metadata": {}, "name": "example", "organization_id": "11111111-1111-4111-8111-111111111111", "slug": "example", "status": "Active", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.tenants().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().tenants().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/tenants");
}

AXIAM_TEST("management users: client.users() and management().users() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "email": "example", "email_verified": true, "failed_login_attempts": 1, "id": "11111111-1111-4111-8111-111111111111", "is_locked": true, "locked_until": "2026-08-26T00:00:00Z", "metadata": {}, "mfa_enabled": true, "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "username": "example"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "email": "example", "email_verified": true, "failed_login_attempts": 1, "id": "11111111-1111-4111-8111-111111111111", "is_locked": true, "locked_until": "2026-08-26T00:00:00Z", "metadata": {}, "mfa_enabled": true, "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "username": "example"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.users().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().users().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/users");
}

AXIAM_TEST("management groups: client.groups() and management().groups() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.groups().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().groups().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/groups");
}

AXIAM_TEST("management roles: client.roles() and management().roles() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "is_global": true, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "is_global": true, "name": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.roles().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().roles().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/roles");
}

AXIAM_TEST("management permissions: client.permissions() and management().permissions() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"action": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"action": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.permissions().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().permissions().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/permissions");
}

AXIAM_TEST("management resources: client.resources() and management().resources() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "uma_registered_by": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "metadata": {}, "name": "example", "parent_id": "11111111-1111-4111-8111-111111111111", "resource_type": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "uma_registered_by": "example", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.resources().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().resources().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/resources");
}

AXIAM_TEST("management scopes: client.scopes() and management().scopes() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json([{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}])json",
                                              200, R"json([{"created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}])json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.scopes().list("11111111-1111-4111-8111-111111111111");
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().scopes().list("11111111-1111-4111-8111-111111111111");
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/resources/11111111-1111-4111-8111-111111111111/scopes");
}

AXIAM_TEST("management service_accounts: client.service_accounts() and management().service_accounts() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "description": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.service_accounts().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().service_accounts().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/service-accounts");
}

AXIAM_TEST("management certificates: client.certificates() and management().certificates() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"cert_type": "User", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"cert_type": "User", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "issuer_ca_id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "metadata": {}, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.certificates().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().certificates().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/certificates");
}

AXIAM_TEST("management ca_certificates: client.ca_certificates() and management().ca_certificates() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "key_custody": "example", "key_locator": "example", "mtls_trust_anchor": true, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "organization_id": "11111111-1111-4111-8111-111111111111", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"chain_pem": "example", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "key_algorithm": "Rsa4096", "key_custody": "example", "key_locator": "example", "mtls_trust_anchor": true, "not_after": "2026-08-26T00:00:00Z", "not_before": "2026-08-26T00:00:00Z", "organization_id": "11111111-1111-4111-8111-111111111111", "parent_ca_id": "11111111-1111-4111-8111-111111111111", "public_cert_pem": "example", "status": "Active", "subject": "example", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.ca_certificates().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().ca_certificates().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/ca-certificates");
}

AXIAM_TEST("management pgp_keys: client.pgp_keys() and management().pgp_keys() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"algorithm": "Rsa4096", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "public_key_armored": "example", "purpose": "AuditSigning", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"algorithm": "Rsa4096", "created_at": "2026-08-26T00:00:00Z", "fingerprint": "example", "id": "11111111-1111-4111-8111-111111111111", "name": "example", "public_key_armored": "example", "purpose": "AuditSigning", "status": "Active", "tenant_id": "11111111-1111-4111-8111-111111111111"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.pgp_keys().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().pgp_keys().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/pgp-keys");
}

AXIAM_TEST("management webhooks: client.webhooks() and management().webhooks() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "enabled": true, "events": ["example"], "id": "11111111-1111-4111-8111-111111111111", "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "url": "example"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "enabled": true, "events": ["example"], "id": "11111111-1111-4111-8111-111111111111", "retry_policy": {"backoff_multiplier": 1.5, "initial_delay_secs": 1, "max_retries": 1}, "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z", "url": "example"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.webhooks().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().webhooks().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/webhooks");
}

AXIAM_TEST("management oauth2_clients: client.oauth2_clients() and management().oauth2_clients() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "id": "11111111-1111-4111-8111-111111111111", "jwks": "example", "jwks_uri": "example", "name": "example", "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"client_id": "example", "created_at": "2026-08-26T00:00:00Z", "dpop_bound_access_tokens": true, "dpop_require_nonce": true, "grant_types": ["example"], "id": "11111111-1111-4111-8111-111111111111", "jwks": "example", "jwks_uri": "example", "name": "example", "profile": "standard", "redirect_uris": ["example"], "require_par": true, "scopes": ["example"], "self_signed_tls_client_auth_thumbprints": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "tls_client_auth_san_dns": "example", "tls_client_auth_san_uri": "example", "tls_client_auth_subject_dn": "example", "tls_client_certificate_bound_access_tokens": true, "token_endpoint_auth_method": "client_secret_post", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.oauth2_clients().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().oauth2_clients().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/oauth2-clients");
}

AXIAM_TEST("management federation: client.federation() and management().federation() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"attribute_map": {}, "client_id": "example", "created_at": "2026-08-26T00:00:00Z", "enabled": true, "id": "11111111-1111-4111-8111-111111111111", "metadata_url": "example", "protocol": "example", "provider": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"attribute_map": {}, "client_id": "example", "created_at": "2026-08-26T00:00:00Z", "enabled": true, "id": "11111111-1111-4111-8111-111111111111", "metadata_url": "example", "protocol": "example", "provider": "example", "tenant_id": "11111111-1111-4111-8111-111111111111", "token_exchange": {"accepted_audiences": ["example"], "enabled": true, "max_lifetime_secs": 1, "max_token_age_secs": 1, "scope_map": {}, "subject_mapping": "example"}, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.federation().list_configs();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().federation().list_configs();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/federation-configs");
}

AXIAM_TEST("management notification_rules: client.notification_rules() and management().notification_rules() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["login_failure"], "id": "11111111-1111-4111-8111-111111111111", "name": "example", "recipient_emails": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["login_failure"], "id": "11111111-1111-4111-8111-111111111111", "name": "example", "recipient_emails": ["example"], "tenant_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.notification_rules().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().notification_rules().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/notification-rules");
}

AXIAM_TEST("management email_config: client.email_config() and management().email_config() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"created_at": "2026-08-26T00:00:00Z", "enabled": true, "from_email": "example", "from_name": "example", "id": "11111111-1111-4111-8111-111111111111", "provider": {"kind": "smtp", "host": "example", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example", "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json",
                                              200, R"json({"created_at": "2026-08-26T00:00:00Z", "enabled": true, "from_email": "example", "from_name": "example", "id": "11111111-1111-4111-8111-111111111111", "provider": {"kind": "smtp", "host": "example", "port": 1, "starttls": true, "username": "example"}, "reply_to": "example", "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "updated_at": "2026-08-26T00:00:00Z"})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.email_config().get_org();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().email_config().get_org();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/email-config");
}

AXIAM_TEST("management settings: client.settings() and management().settings() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"certificate": {"default_cert_validity_days": 1, "max_cert_validity_days": 1}, "created_at": "2026-08-26T00:00:00Z", "email": {"email_verification_grace_period_hours": 1, "email_verification_required": true}, "id": "11111111-1111-4111-8111-111111111111", "lockout": {"lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1}, "mfa": {"mfa_challenge_lifetime_secs": 1, "mfa_enforced": true}, "notification": {"admin_notifications_enabled": true}, "opaque": {"opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example"}, "password": {"hibp_check_enabled": true, "min_length": 1, "password_history_count": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true}, "privacy": {"deletion_grace_period_days": 1}, "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "token": {"access_token_lifetime_secs": 1, "refresh_token_lifetime_secs": 1}, "updated_at": "2026-08-26T00:00:00Z"})json",
                                              200, R"json({"certificate": {"default_cert_validity_days": 1, "max_cert_validity_days": 1}, "created_at": "2026-08-26T00:00:00Z", "email": {"email_verification_grace_period_hours": 1, "email_verification_required": true}, "id": "11111111-1111-4111-8111-111111111111", "lockout": {"lockout_backoff_multiplier": 1.5, "lockout_duration_secs": 1, "max_failed_login_attempts": 1, "max_lockout_duration_secs": 1}, "mfa": {"mfa_challenge_lifetime_secs": 1, "mfa_enforced": true}, "notification": {"admin_notifications_enabled": true}, "opaque": {"opaque_ksf": "example", "opaque_mode": "example", "opaque_suite": "example"}, "password": {"hibp_check_enabled": true, "min_length": 1, "password_history_count": 1, "require_digits": true, "require_lowercase": true, "require_symbols": true, "require_uppercase": true}, "privacy": {"deletion_grace_period_days": 1}, "scope": "Org", "scope_id": "11111111-1111-4111-8111-111111111111", "token": {"access_token_lifetime_secs": 1, "refresh_token_lifetime_secs": 1}, "updated_at": "2026-08-26T00:00:00Z"})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.settings().get_org();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().settings().get_org();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/organizations/11111111-1111-4111-8111-111111111111/settings");
}

AXIAM_TEST("management scim_tokens: client.scim_tokens() and management().scim_tokens() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json([{"created_at": "2026-08-26T00:00:00Z", "created_by": "11111111-1111-4111-8111-111111111111", "expires_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "last_used_at": "2026-08-26T00:00:00Z", "name": "example", "revoked_at": "2026-08-26T00:00:00Z", "status": "active", "tenant_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"}])json",
                                              200, R"json([{"created_at": "2026-08-26T00:00:00Z", "created_by": "11111111-1111-4111-8111-111111111111", "expires_at": "2026-08-26T00:00:00Z", "id": "11111111-1111-4111-8111-111111111111", "last_used_at": "2026-08-26T00:00:00Z", "name": "example", "revoked_at": "2026-08-26T00:00:00Z", "status": "active", "tenant_id": "11111111-1111-4111-8111-111111111111", "user_id": "11111111-1111-4111-8111-111111111111"}])json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.scim_tokens().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().scim_tokens().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/scim-tokens");
}

AXIAM_TEST("management reactors: client.reactors() and management().reactors() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "id": "11111111-1111-4111-8111-111111111111", "last_seen_at": "2026-08-26T00:00:00Z", "mode": "intercept", "name": "example", "priority": 1, "recent_timeout_count": 1, "recent_veto_count": 1, "tenant_id": "11111111-1111-4111-8111-111111111111", "timeout_ms": 1, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"created_at": "2026-08-26T00:00:00Z", "description": "example", "enabled": true, "events": ["example"], "failure_policy": "fail_closed", "id": "11111111-1111-4111-8111-111111111111", "last_seen_at": "2026-08-26T00:00:00Z", "mode": "intercept", "name": "example", "priority": 1, "recent_timeout_count": 1, "recent_veto_count": 1, "tenant_id": "11111111-1111-4111-8111-111111111111", "timeout_ms": 1, "updated_at": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.reactors().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().reactors().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/reactors");
}

AXIAM_TEST("management webauthn_policy: client.webauthn_policy() and management().webauthn_policy() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"allowed_aaguids": ["11111111-1111-4111-8111-111111111111"], "block_revoked_status": true, "blocked_aaguids": ["11111111-1111-4111-8111-111111111111"], "min_certification": "L1", "mode": "none", "require_fido_certified": true, "unknown_aaguid": "allow", "effective_unknown_aaguid": "allow"})json",
                                              200, R"json({"allowed_aaguids": ["11111111-1111-4111-8111-111111111111"], "block_revoked_status": true, "blocked_aaguids": ["11111111-1111-4111-8111-111111111111"], "min_certification": "L1", "mode": "none", "require_fido_certified": true, "unknown_aaguid": "allow", "effective_unknown_aaguid": "allow"})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.webauthn_policy().get();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().webauthn_policy().get();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/tenants/11111111-1111-4111-8111-111111111111/webauthn/attestation-policy");
}

AXIAM_TEST("management audit: client.audit() and management().audit() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"items": [{"action": "example", "actor_id": "11111111-1111-4111-8111-111111111111", "actor_type": "User", "id": "11111111-1111-4111-8111-111111111111", "ip_address": "example", "metadata": {}, "outcome": "Success", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "timestamp": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json",
                                              200, R"json({"items": [{"action": "example", "actor_id": "11111111-1111-4111-8111-111111111111", "actor_type": "User", "id": "11111111-1111-4111-8111-111111111111", "ip_address": "example", "metadata": {}, "outcome": "Success", "resource_id": "11111111-1111-4111-8111-111111111111", "tenant_id": "11111111-1111-4111-8111-111111111111", "timestamp": "2026-08-26T00:00:00Z"}], "total": 1, "offset": 0, "limit": 50})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.audit().list();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().audit().list();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/audit-logs");
}

AXIAM_TEST("management privacy: client.privacy() and management().privacy() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(204, "",
                                              204, "");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.privacy().request_export();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().privacy().request_export();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/api/v1/account/export");
}

AXIAM_TEST("management platform: client.platform() and management().platform() are equivalent (§27.2 rule 4)") {
    auto fixture = axtest::mgmt::signed_in_two(200, R"json({"status": "example"})json",
                                              200, R"json({"status": "example"})json");

    // §27.2 rule 4: "where an SDK offers both, the two MUST return equivalent handles".
    // Equivalent means the same request, not merely the same type -- so this compares the
    // method and path each actually put on the wire.
    (void) fixture.client.platform().health();
    const auto direct_method = fixture.state->last().method;
    const auto direct_path = axtest::mgmt::path_of(fixture.state->last().url);

    (void) fixture.client.management().platform().health();
    AXIAM_CHECK(fixture.state->last().method == direct_method);
    AXIAM_CHECK(axtest::mgmt::path_of(fixture.state->last().url) == direct_path);
    AXIAM_CHECK(direct_path == "/health");
}

// §27.9: 114 models, 23 enums and 24 namespaces are covered above. Counted from the test
// registry rather than restated as a literal on both sides -- a case dropped by a bad
// regeneration would still satisfy a tautology, and fails this instead.
AXIAM_TEST("the generated model suite covers every model, enum and namespace") {
    int round_trips = 0;
    int enum_maps = 0;
    int rescopes = 0;
    int equivalents = 0;
    for (const auto& test : axtest::registry()) {
        if (test.name.find("round-trips without losing a field") != std::string::npos) {
            ++round_trips;
        } else if (test.name.find("maps every value both ways") != std::string::npos) {
            ++enum_maps;
        } else if (test.name.find("re-scoping returns a new handle") != std::string::npos) {
            ++rescopes;
        } else if (test.name.find("are equivalent") != std::string::npos) {
            ++equivalents;
        }
    }
    AXIAM_CHECK(round_trips == 114);
    AXIAM_CHECK(enum_maps == 23);
    AXIAM_CHECK(rescopes == 24);
    AXIAM_CHECK(equivalents == 24);
}

}  // namespace
